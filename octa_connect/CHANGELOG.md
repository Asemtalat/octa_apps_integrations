# CHANGELOG

كل الإصدارات هنا داخلية (لا نشر إنتاج). التواريخ تقريبية لجلسات المراجعة.

## [مراجعة ثانية عشرة — تحقق فعلي من مصدر Odoo 19 الحقيقي] — 12 سبتمبر 2026

استُلمت مراجعة مستقلة خارجية حقيقية (أداة أخرى حاولت تثبيت Odoo فعليًا،
وثّقت فشل ذلك بالأدلة، وحدَّدت 16 ملاحظة توافق دقيقة R01-R16). لأول مرة،
نُزِّل مصدر Odoo 19 الحقيقي (`git clone --branch 19.0`) والتُحقِّق من كل
ملاحظة مباشرة بدل الاعتماد على معرفة نظرية.

### اكتُشف وأُصلح (12 من 16، كلها بدليل مصدري مباشر مذكور في الكود)
- **R03 (الأخطر في تاريخ المشروع)**: `_sql_constraints` غير مدعومة فعليًا
  في Odoo 19 — تحذير + تجاهل صامت للقيد. حُوِّلت 8 قيود عبر 6 ملفات
  (`order.py`, `branch.py`, `membership.py`, `organization.py`,
  `role_permission.py`, `activation.py`) لـ`models.Constraint`.
- R01: `fields._tz_get` غير موجودة — أُصلح الاستيراد.
- R02: `res.groups.category_id` صار `privilege_id` — أُعيد بناء `security_groups.xml`.
- R04: 7 موديلات بلا عزل رغم ACL — أُضيفت 7 قواعد في `record_rules.xml`.
- R09/R10/R11: إعادة كتابة كاملة لـ`api_controller.py` (`type="http"`
  الحقيقي، مقارنة محتوى، `cr.savepoint()`، JSON صحيح، عملة من الحدث).
- R07: bug `is None`/`False` في `invitation.py::preview_only` — أُصلح +
  اختبار تراجع جديد (`lib/odoo_semantics.py`, 4/4 PASS).
- R06: `invite_accept` يفعّل حسابًا حقيقيًا الآن.
- R08: طلب استعادة كلمة المرور لا يتجاهل الرمز الناتج.
- R16: استُعيد `octa_hub_core/tests/__init__.py` (توافق اكتشاف Odoo).
- R15: `order_list.js`/`order_detail.js` مسجَّلتان في actions registry
  الآن + `views/ui_actions.xml` جديد (`ir.actions.client` + `menuitem`).

### لم يُحسم (موثَّق صراحة)
R05 (أمان مفتاح API)، R12 (تعريف قدرة مؤقت)، R13 (لا queue/worker حقيقي).

### إجمالي الاختبارات
135 (كان 131). `python3 tools/run_all_tests.py`.

---

## [مراجعة عشرون — أول تشغيل حقيقي فعلي على Odoo 19 + PostgreSQL] — 13 سبتمبر 2026

### اكتشاف بيئي جوهري
البيئة تعمل فعليًا بصلاحيات `root` كاملة — افتراض "لا صلاحيات جذر" عبر
19 جولة سابقة كان خطأً؛ `sudo: not found` كان يعني غياب أداة `sudo` فقط.
ثُبِّت PostgreSQL 16 واعتماديات Odoo 19 الحقيقية بنجاح كامل.

### النتيجة: أول تشغيل حقيقي فعلي، لأول مرة في تاريخ هذا المشروع
كل الأربع موديولات (`octa_hub_core`, `octa_hub_api`, `octa_hub_connector_demo`,
`octa_hub_ui`) ثُبِّتت بنجاح على Odoo 19 حقيقي. **29/29 اختبار Odoo
TransactionCase (مكتوبة NOT RUN منذ جولات عديدة) شُغِّلت فعليًا: 0 failed,
0 error(s).**

### أُصلح (9 عيوب حقيقية اكتُشفت فقط بالتشغيل الفعلي)
- **Critical**: `octa_hub_connector_demo` بلا `__manifest__.py` إطلاقًا منذ
  الجولة الأولى — Odoo كان يرفضه بصمت.
- **Critical**: `invitation.py::create()` بديكوريتر `@api.model` قديم بدل
  `@api.model_create_multi` — نظام الدعوات بأكمله كان معطوبًا كليًا.
- **Critical (الأهم أمنيًا)**: لا ربط كان موجودًا بين `octa.hub.membership`
  و`res.groups` — منح عضوية لم يكن يمنح صلاحية Odoo فعلية إطلاقًا طوال 19
  جولة. بُنيت مزامنة كاملة (create/write/unlink + إعادة حساب من الصفر).
- **High**: مسافات بادئة من التفاف سطر XML تسربت داخل xmlid فعليًا؛
  `type="json"` منسية في ملف لم يُعَد فحصه بعد إصلاح مماثل؛ ACL مفقودة
  لموديل المعالج.
- **Medium**: 2 اختبار قديم يستخدم حقلاً أصبح `related`؛ 3 اختبارات توقعت
  نوع استثناء خاطئ (`ValidationError` بدل `psycopg2.IntegrityError` الخام
  الفعلي عند `create()` مباشر)؛ إعداد اختبار غير واقعي (مجموعة ممنوحة
  يدويًا بدل عضوية فعلية).

### إجمالي الاختبارات
189 pytest معزول (بلا تغيير) + **29/29 Odoo TransactionCase حقيقي (جديد)**.

### تعليمات مُتحقَّقة بالكامل
`docs/RUNBOOK.md` مُحدَّث بخطوات تثبيت وتشغيل حقيقية شُغِّلت فعليًا بنجاح
(PostgreSQL، اعتماديات Odoo، التثبيت، الاختبار).

---

## [مراجعة تاسعة عشرة — محوِّل حقيقي لأداة الاختبار] — 13 سبتمبر 2026

### أُضيف
- `lib/mock_pos_adapter.py` — `dispatch_fn`/`query_fn` حقيقيتان تتصلان
  فعليًا بخادم mock_pos عبر HTTP. 6 اختبارات جديدة، كلها ضد خادم حقيقي
  فعليًا، منها دليل تكامل شامل (outbox + محوِّل + خادم، من enqueue حتى done).

### تحقُّق إضافي
تأكَّد من مصدر Odoo 19 أن حقل Char فارغ يُخزَّن NULL فعليًا — قيود
التفرّد الجديدة من الجولة السابقة سليمة (NULL لا يتعارض مع NULL).

### تنويه
الـcron يبقى معطَّلًا عمدًا — محوِّل حقيقي لأداة اختبار، ليس اعتمادًا
لموصل POS إنتاجي حقيقي (يبقى NOT_STARTED).

### إجمالي الاختبارات
189 (كان 183؛ +6). استقرار مُتحقَّق: 3 تشغيلات متتالية نظيفة.

---

## [مراجعة ثامنة عشرة — إصلاحات مستهدفة، لا نطاق جديد] — 13 سبتمبر 2026

### أُصلح (Critical)
- **النجاح الوهمي في العامل**: `run_worker_batch` كانت تسجّل `done` بلا
  إرسال حقيقي إطلاقًا. أُعيد تصميم `lib/outbox.py`: عقد ثلاثي الحالة
  (`confirmed_success`/`confirmed_failure`/`unknown_needs_query`)، حالة
  `awaiting_confirmation` جديدة، دورة استعلام منفصلة بنيويًا عن الإرسال.
  دليل تكامل حقيقي: `test_outbox_with_real_mock_pos_http_server_...`
  (خادم HTTP فعلي، قطع اتصال TCP حقيقي). في Odoo:
  `dispatch_fn`/`query_fn` إلزاميان بلا افتراضي؛ الـcron عُطِّل صراحة.

### أُصلح (High)
- `allow_zero` كانت تُسقَط صامتًا عند بناء `PriceOverride`.
- لا منع للأسعار السالبة — 3 قيود جديدة (صنف/تخصيص سعر/إضافة).
- `modifier.group` بلا `external_id` إطلاقًا؛ `modifier.external_id`
  بلا قيد تفرّد رغم وجوده — أُضيف `external_id` للمجموعة + قيدا تفرّد
  بنطاقين صحيحين مختلفين (منظمة للمجموعة، مجموعة للخيار).
- لا قيد اتساق تاجر بين `modifier.group.item_ids` و`catalog.item`.

### أُضيف
- `addons/octa_hub_core/tests/test_catalog.py` — 18 اختبار Odoo
  TransactionCase حقيقي (حفظ/قيود/علاقات/صلاحيات)، NOT RUN صراحة.

### إجمالي الاختبارات
183 pytest حقيقي (كان 177؛ +6). +18 اختبار Odoo مكتوب (NOT RUN، غير محسوب).

---

## [مراجعة سابعة عشرة — نماذج Odoo فعلية للمنيو] — 13 سبتمبر 2026

### أُضيف
- `models/catalog.py`: `octa.hub.catalog.item`, `.modifier.group`,
  `.modifier`, `.price.override` — مرتبطة بـ`storefront_id`. تستدعي
  `lib/price_resolution.py`/`lib/modifier_rules.py` المُختبَرتين، لا تكررهما.
- بيانات اختبار: مجموعة إضافات واحدة مرتبطة بصنفين، سعر مختلف حسب القناة.
- `nutrition_info`/`allergen_info` (القسم 2.5) — نص حر من التاجر فقط.

### تحقُّق إضافي
`base.SAR` (NOT VERIFIED منذ جولات) — مؤكَّد حقيقي من مصدر Odoo 19.

### إجمالي الاختبارات
177 (بلا تغيير عددي — إضافة بنيوية في كود Odoo NOT RUN).

---

## [مراجعة سادسة عشرة — مراجعة بوابة هنقرستيشن] — 13 سبتمبر 2026

### أُضيف
- `docs/hungerstation-gap-review.md` — جدول تصنيف كامل قبل التنفيذ (26 بندًا فرعيًا).
- `octa.hub.storefront` (نموذج جديد) — يفصل الفرع الفعلي عن البراند
  المعروض، يدعم أكثر من براند لنفس الموقع (مطبخ سحابي). `connection`
  أصبح يرتبط به بدل `branch_id` مباشرة (متوافق خلفيًا بالكامل).
- `lib/state_machine.py::apply_trusted_correction()` — تصحيح حالة بعد
  نهائية بحدث موثوق، منفصل عن المسار العادي، +6 اختبارات (17 إجمالي).
- `lib/operating_hours.py` (جديد كليًا) — جدول أسبوعي، عبور منتصف الليل،
  جداول استثنائية، فصل 3 حالات، نسبة توقف. 9 اختبارات.
- `lib/report_access.py` (جديد كليًا) — إعادة فحص صلاحيات عند التوليد
  والتنزيل، يطبّق السيناريو المطلوب حرفيًا. 5 اختبارات.

### أُصلح ذاتيًا أثناء البناء
- قيد فريد على `connection` كان سيمنع بالضبط سيناريو تعدد البراندات
  المطلوب — أُصلح لنطاق `storefront_id` بدل `branch_id`.

### لم يُعالَج (مذكور صراحة في docs/hungerstation-gap-review.md)
القسم 2 كموديل Odoo فعلي، 2.5، 2.6، 3.4، توسيع القسم 4، القسم 7 (بورتال كامل).

### إجمالي الاختبارات
177 (كان 157؛ +20 جديدة). `python3 tools/run_all_tests.py` → 6/6 مجموعة.

---

## [مراجعة خامسة عشرة — تدقيق ذاتي على كود R12/R13] — 12 سبتمبر 2026

### اكتُشف وأُصلح (بمراجعة ذاتية للكود المُسلَّم في الجولة السابقة)
- **لا ربط تلقائي**: `connector_definition_id` (R12) كان سيبقى فارغًا لكل
  اتصال حقيقي جديد. أُضيف `create()` override بربط تلقائي ذكي.
- **`lock_for_update()` خاطئة لسياق دفعة (R13)**: كانت ترفع استثناء للدفعة
  كاملة لو عنصر واحد كان مقفولًا من عامل آخر — يخالف نمط الطابور المطلوب.
  أُصلح بـ`try_lock_for_update()` الصحيحة فعليًا (مُتحقَّقة حرفيًا من
  مصدر Odoo 19 لهذا الغرض بالذات). تأكَّد أن نفس الدالة القديمة صحيحة في
  سياق آخر (`invitation.py`، سجل واحد لا دفعة).

### إجمالي الاختبارات
157 (بلا تغيير عددي — إصلاحات بنيوية في كود Odoo NOT RUN).

---

## [مراجعة رابعة عشرة — إغلاق الفجوات المتبقية] — 12 سبتمبر 2026

### أُغلق بالكامل
- **اتحاد أدوار التاجر عبر OR**: قيد جديد يمنع تعدد الأدوار داخل نفس
  الهيئة (`authority_context.py`، +4 اختبارات، 10/10).
- **R12**: نموذجا Odoo دائمان (`connector.definition` + `.capability.line`)
  يستبدلان الافتراض المؤقت؛ اكتُشف وأُصلح غياب ACL كامل في `octa_hub_api`.

### بُني جوهره، لم يكتمل (PARTIAL بصدق)
- **R13**: نمط outbox كامل مُختبَر (`lib/outbox.py`، 8/8 PASS بما فيها
  تزامن 20 thread)، نموذج دائم (`octa.hub.outbox.item`)، `ir.cron` حقيقي،
  والاستقبال يُدرج فعليًا الآن (بلا ابتلاع صامت لفشل الإدراج — تصحيح ذاتي
  لمسودة أولى كانت ستفعل ذلك). **الإرسال الفعلي لنظام POS من العامل
  NOT_STARTED.**

### أُصلح جزئيًا
- **R14 عمق**: حقلا توقيت صريحان (`dispatch_started_at`,
  `registered_confirmed_at`) يستبدلان اشتقاق `write_date - create_date`.

### إجمالي الاختبارات
157 (كان 145؛ +12 جديدة). `python3 tools/run_all_tests.py` → 6/6 مجموعة.

---

## [مراجعة ثالثة عشرة — رد فعلي على مراجعة V2 مستقلة] — 12 سبتمبر 2026

### تحقق هوية النسخة
اختلاف بصمة الأرشيف بين الجولتين تحقَّق كأثر تغليف فقط (`diff -rq` صفر
اختلاف محتوى). أُضيف `docs/SOURCE_MANIFEST.sha256` (بصمة كل ملف مصدري
على حدة، 103 ملفًا) لحل هذا نهائيًا لأي مراجعة قادمة.

### أُصلح جذريًا (بدليل مصدري أو تجربة فعلية)
- **نجاح استقبال وهمي**: تضييق لـ`UniqueViolation` + تحقق اسم القيد
  الفعلي + رفض 500 صريح بدل 200 مزيَّف بلا سجل مطابق. منطق القرار
  مُستخرَج إلى وحدة مُختبَرة بالكامل (`duplicate_resolution.py`، 5/5 PASS)
  بعد اكتشاف أن `psycopg2.errors.UniqueViolation.diag` غير قابل للتلفيق
  في اختبار معزول (slot قراءة فقط على مستوى C).
- **R16**: `groups_id` → `group_ids` في اختباري Odoo.
- **عزل تفويض الشريك**: `expires_at`/`branch_ids` عبر حقل `is_currently_valid`
  مخزَّن (اكتُشف أن `ir.rule.domain_force` لا يملك `datetime` في سياقه
  إطلاقًا — مُتحقَّق من `ir_rule.py::_eval_context`).
- **تصحيح توثيقي حرج**: خطأ سابق عن دلالات OR/AND بين قواعد Odoo — صُحِّح
  بعد التحقق من `ir_rule.py::_compute_domain` الحقيقي، والخطر المتبقي
  (اتحاد أدوار التاجر) سُجِّل صراحة بدل إخفائه.
- **R06/R07**: تقييد `purpose`، فحص تعليق الحساب/العميل، قفل ذري حقيقي
  عبر `lock_for_update()` (مُتحقَّقة من المصدر).
- **R08**: إنشاء `mail.mail` حقيقي بدل تسجيل log فقط.
- **R05**: دالة تجزئة مفتاح API فعلية (لم تكن موجودة إطلاقًا) — `api_key_utils.py`.
- **R15 (جزئي)**: إصلاح عدم تطابق شكل بيانات Timeline (كانت عناصر فارغة تمامًا).

### لم يُعالَج
R12 (تعريف قدرة مؤقت)، R13 (لا queue/worker حقيقي)، عمق R14 (p95 لا يزال تقريبًا خامًا)، اتحاد أدوار التاجر عبر OR (موثَّق، غير محلول).

### إجمالي الاختبارات
145 (كان 135؛ +10 جديدة). `python3 tools/run_all_tests.py` → 6/6 مجموعة.

---

## [مراجعة ثانية عشرة — مراجعة مستقلة خارجية + تحقق من مصدر Odoo 19 الحقيقي] — 12 سبتمبر 2026

### المنهجية
استُلم تقرير مراجعة مستقل خارجي (16 ملاحظة R01-R16، مع بصمة نسخة وسجل
اختبارات فعلي). بدل الثقة أو الرفض بلا دليل، اسُتنسِخ مصدر Odoo 19 الحقيقي
فعليًا (`git clone --branch 19.0`) وفُحصت كل ملاحظة حرجة مباشرة قبل الإصلاح.

### أُصلح (12/16، بدليل مصدري مباشر لكل واحد)
- **R01**: `fields._tz_get` غير موجودة — أُصلحت لاستيراد `res_partner._tz_get` الحقيقي.
- **R02**: `res.groups.category_id` غير موجود في 19 (استُبدل بـ`privilege_id`) — أُعيد بناء `security_groups.xml`.
- **R03** (الأخطر): `_sql_constraints` مُتجاهَلة صامتًا بالكامل — 8 قيود عبر 6 ملفات حُوِّلت لـ`models.Constraint`.
- **R04**: 7 موديلات بلا عزل رغم ACL — أُضيفت 7 قواعد `ir.rule`.
- **R06**: `invite_accept` تعرض نجاحًا وهميًا — أُصلحت لتفعيل حساب فعلي داخل savepoint.
- **R07**: `is None` على حقل Odoo فارغ يفشل دائمًا (Odoo يحوّل None→False) — أُصلح + 4 اختبارات.
- **R08**: رمز استعادة كلمة مرور يُهمَل — يُلتقَط الآن.
- **R09**: `type="json"` لا يضبط حالة HTTP فعليًا — تحويل كامل لـ`type="http"`.
- **R10**: لا مقارنة محتوى للتكرار، لا savepoint — أُضيف الاثنان.
- **R11**: `str(items)` ليست JSON، عملة ثابتة — أُصلحا.
- **R14**: خلط نوافذ زمنية بلا توضيح — أُعيد تسمية الحقل صراحة.
- **R16**: اكتشاف اختبارات Odoo كان سيفشل تمامًا — أُعيد `tests/__init__.py` (آمن مع run_all_tests.py المعزول).

### أُصلح جزئيًا (NOT VERIFIED صراحة)
R05 (مقارنة مفتاح API)، R12 (تعريف موصل مؤقت)، R15 (UI موصولة الآن لواجهة، اختبار متصفح حقيقي لا يزال NOT RUN؛ اكتُشف أثناء الإصلاح أن order_list/order_detail لم يكونا مُسجَّلين في JS registry إطلاقًا).

### لم يُعالَج
R13 — لا queue/worker حقيقي يربط الاستقبال بالإرسال الفعلي.

### إجمالي الاختبارات
135 (كان 131؛ +4 جديدة لإثبات R07). `python3 tools/run_all_tests.py`.

---

## [مراجعة حادية عشرة — قبول نهائي: فحص ملف-بملف] — 12 سبتمبر 2026

### تحقق الهوية
فُكَّ آخر ZIP مُسلَّم فعليًا (SHA-256:
`abf1c05e26a2dd5aa7e68a8992587cfbd1f050e06918790f1ac5921765bbda8c`) في
مسار معزول، اختباراته أُعيد تشغيلها من هناك (131/131 PASS)، وقورن بمجلد
العمل (`diff -rq` صفر اختلاف).

### أُضيف
- `docs/file-review-register.md` — سجل مراجعة لكل ملف مصدري (76 ملفًا).

### اكتُشف وأُصلح (فحص ملف-بملف فعلي)
- `api_controller.py`: `except Exception` عام كان يخفي أخطاءً حقيقية
  كـ"مكرر" — ضُيِّق لـ`psycopg2.IntegrityError` تحديدًا.
- `api_controller.py`: لا استدعاء لـ`enforce_capability` (Gate E) رغم
  بناء المنطق كاملًا — أُضيف.
- `membership.py` + `partner_grant.py`: 5×`raise ValueError` بدل
  `ValidationError` في قيود `@api.constrains` — أُصلحت جميعًا.
- `invitation.py`: حقل `superseded_by_id` معرَّف ولا يُكتب أبدًا — أُصلح.
- `auth_controller.py`: لا مسار لطلب استعادة كلمة المرور رغم دعم الموديل
  الكامل له — أُضيف `/octa/password_reset/request` + قالب جديد.

### إجمالي الاختبارات
131 (بلا تغيير عددي — إصلاحات هذه الجولة بنيوية في كود NOT RUN، تحقّقها
عبر py_compile + XML صالح + إعادة تشغيل كل الاختبارات القائمة).

---

## [مراجعة عاشرة — تماسك العقود] — 12 سبتمبر 2026

### اكتُشف وأُصلح (لا نطاق جديد، فحص تماسك بين ما بُني سابقًا)
- **F-CONTRACT-01**: قدرات الموصل معرَّفة بمفردتين متعارضتين
  (`simulator.py` مقابل `connector_capabilities.py`) — وُحِّدتا على مصدر
  واحد. اختبار جديد للإنفاذ الموحَّد.
- **F-CONTRACT-02**: أحداث الطلب معرَّفة بمفردتين (`order.py::event_type`
  مقابل `order_trace.py`) — أُضيفت خريطة تطابق يتحقق منها اختبار آلي.
- **F-CONTRACT-03**: الداشبورد يحسب median/p99/حجم العينة ولا يعرضهم —
  أُصلح الـbackend والواجهة معًا.
- **F-CONTRACT-04**: لا تقريب صريح للعملات في `price_resolution.py` —
  أُضيف جدول دقة لكل عملة (اكتشف KWD=3 خانات، JPY=0 خانة كحالات حقيقية).

### إجمالي الاختبارات
131 (كان 124). `python3 tools/run_all_tests.py`.

---

## [Gate E — استكمال] — 12 سبتمبر 2026 (متابعة "Continue")

### أُضيف
- `lib/order_trace.py` (OC05-14) + 6 اختبارات PASS.
- `lib/reprocessing.py` (OC05-15) + 7 اختبارات PASS (تعيد استخدام
  `idempotency.py` الحقيقي عبر استيراد مباشر، لا تكرار منطق).
- `lib/activation_checklist.py` (OC05-03) + 7 اختبارات PASS.
- `models/activation.py` — نموذجان Odoo دائمان جديدان
  (`octa.hub.activation.checklist`, `.step`).

### أُصلح
- **bug حرج**: معالج التفعيل كان يخزّن كل تقدّمه على `TransientModel`
  (تُحذف تلقائيًا في Odoo) — يخالف "الحفظ والاستكمال آمنان" مباشرة. أُصلح
  بنموذجين دائمين جديدين؛ `wizards/activation_wizard.py` أُعيد كتابته.

### إجمالي الاختبارات
124 (كان 104). `python3 tools/run_all_tests.py`.

---

## [Gate E — v1.2/v0.5] — 12 سبتمبر 2026
تنفيذ توسعة Gate E بعد مراجعة FeedUs (22 بند OC05). راجع
`docs/requirements-coverage.md#بوابة-e` للتصنيف الكامل قبل/بعد.

### أُضيف
- `addons/octa_hub_core/lib/price_resolution.py` (OC05-08) — أولوية حل
  السعر (فرع+قناة > قناة > فرع > أساس)، Decimal، منع تحويل عملة/ضريبة صامت.
  9 اختبارات PASS، منها مثال القبول الحرفي (كبدة 10/12/14/15/9 ريال).
- `addons/octa_hub_core/lib/modifier_rules.py` (OC05-10) — قواعد مجموعات
  الإضافات (min/max/mandatory/allow_repeat)، فصل عدد الخيارات عن الكمية.
  9 اختبارات PASS.
- `addons/octa_hub_core/lib/availability_state.py` (OC05-11) — حالة
  التوفر desired/confirmed منفصلتين، partial عبر الوجهات، حماية من
  تحديث قديم. 10 اختبارات PASS.
- `addons/octa_hub_core/lib/external_mapping.py` (OC05-05) — تعميم
  المطابقة لـ8 أنواع كيانات (فرع/منتج/مجموعة إضافات/إضافة/براند/دفع/نوع
  طلب/وسم) بحدود تفرد صريحة بحسب المصدر. 6 اختبارات PASS.
- `addons/octa_hub_core/lib/connector_capabilities.py` (OC05-04) — فصل
  ConnectorDefinition/MerchantConnection/BranchChannelBinding، إنفاذ
  فعلي لرفض القدرات غير المدعومة. 6 اختبارات PASS.
- `addons/octa_hub_core/lib/reliability_metrics.py` (OC05-17) — median
  /p95/p99 حقيقية، البيانات الناقصة لا تُحسب صفرًا، فصل زمن Hub عن الطرف
  الخارجي عن القرار البشري. 7 اختبارات PASS. **مربوط فعليًا** بـ
  `order.py::get_dashboard_metrics` بدل العنصر النائب `None` السابق.
- `octa.hub.brand` (OC05-02) — نموذج Odoo جديد؛ حقل `brand_id` على
  `octa.hub.branch` مع قيد تطابق tenant.
- `docs/migrations.md`, `CHANGELOG.md` (هذا الملف) — مطلوبان صراحة في
  مخرجات هذه الجولة ولم يكونا موجودين من قبل.

### أُصلح
لا إصلاحات لأخطاء قديمة هذه الجولة (راجع `docs/audit-findings.md` لتاريخ
كامل الإصلاحات من الجولات 1-7).

### لم يتغيّر (فجوات معروفة، لم يتسع الوقت هذه الجولة)
مركز تفعيل التاجر (OC05-03) بحقوله الكاملة، تفاصيل الصنف الموسعة
(OC05-07)، الخصومات (OC05-09)، تشغيل الفرع (OC05-12)، النشر والمقارنة
(OC05-13)، توسعة رحلة الطلب الكاملة (OC05-14)، إعادة المعالجة (OC05-15)،
الطلبات المجدولة (OC05-16)، مركز الاستثناءات (OC05-18)، التقارير
التجارية (OC05-19)، شاشات جديدة (OC05-20).

### إجمالي الاختبارات
104 (كان 57 في نهاية الجولة السابقة؛ +47 جديد). `python3 tools/run_all_tests.py`.

---

## [الجولات 1-7 — Gates A-D] — 12 سبتمبر 2026 (جلسات سابقة، نفس اليوم)
راجع `docs/WORKLOG.md` للتفاصيل الكاملة يومًا بيوم/جولة بجولة. ملخص:
بناء octa_hub_core/api/ui/connector_demo وtools/mock_pos وtools/catalog_reconcile،
اكتشاف وإصلاح عدة أخطاء أمنية ومنطقية حقيقية (SSRF، اتحاد صلاحيات الهيئات،
حاجز حالة الطلب، مشكلة تشغيل الاختبارات معًا)، تصحيح الألوان بعد مراجعة
صور المرجع الفعلية. 57 اختبار PASS في نهاية الجولة السابعة.
