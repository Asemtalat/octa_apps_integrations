# سجل العمل (WORKLOG)

## الجلسة 1 (12 سبتمبر 2026)
1. قراءة الملفين كاملين (Phase_1.md كل الأسطر 1-248؛ الـ docx بفقراته 309
   وجداوله الثمانية عبر python-docx).
2. فحص بيئة حقيقي: لا sudo، لا PostgreSQL، لا Odoo، لا متصفح — موثّق بالأدلة.
3. عرضت هذا على المستخدم صراحة قبل أي كود، مع اقتراح Claude Code كبديل.
4. المستخدم أكّد الاستمرار داخل هذه البيئة مع تسجيل NOT RUN بصدق. تابعت.
5. بنيت هيكل المستودع (`addons/`, `tools/`, `docs/`).
6. Gate D: `tools/catalog_reconcile/reconcile.py` + 8 اختبارات → PASS (بعد
   إصلاح خطأ حقيقي واحد في الاختبار نفسه اكتُشف بالتشغيل الفعلي: `_rec()`
   كانت تتجاهل معامل `scope` المُمرَّر — أُصلح وأُعيد التشغيل).
7. Gate B: `addons/octa_hub_api/lib/idempotency.py` + 7 اختبارات تزامن
   حقيقية (20 thread) → PASS.
8. `tools/mock_pos/mock_pos_service.py` (خادم HTTP فعلي) + 6 اختبارات HTTP
   حقيقية → PASS، بما فيها سيناريو "سجّل ثم انقطع الرد".
9. `addons/octa_hub_connector_demo/simulator.py` + اختبار end-to-end كامل
   يربط الثلاثة (connector→idempotency→pos عبر HTTP) → 3/3 PASS.
10. Gate A: كتابة كامل نموذج octa_hub_core (organization, branch,
    membership, role_permission, partner_grant, invitation, audit_log,
    order) + security (groups, ACL, record rules) + controller دخول/دعوة
    + بيانات اختبار + اختبارات Odoo (NOT RUN، مكتوبة فقط).
11. Gate B: عقد OpenAPI (`docs/api/openapi.yaml`) — تحقّق فعلي من صحة YAML.
12. Gate B: controller رقيق يربط النواة المُختبرة بـ Odoo (NOT RUN).
13. Gate C: design tokens SCSS + مكوّن OWL أولي للداشبورد (NOT RUN/NOT
    RENDERED).
14. Gate D: معالج تفعيل (`activation_wizard.py`) يستدعي `reconcile.py`
    المُختبَر (NOT RUN كموديول Odoo، لكن الاستدعاء يعيد استخدام منطق مُثبَت).
15. فحص صياغة شامل: 35 ملف بايثون (`py_compile`، صفر أخطاء)، 4 ملفات XML
    (صفر أخطاء)، YAML (صالح).
16. كتابة `docs/requirements-coverage.md` الشامل (كل بند من الوثيقتين مُرقَّم
    بحالته الحقيقية)، `docs/test-report.md`، `docs/architecture.md`،
    `docs/security.md`، `docs/decisions.md`، `docs/HANDOFF.md` (هذا الملف).
17. التالي: README.md، requirements.txt، .env.example، تجميع ZIP، تسليم.

## الحالة عند نهاية الجلسة
- 24/24 اختبار آلي حقيقي PASS (Gate B core + Gate D core).
- 0 اختبار Odoo فعلي RUN (لا Odoo متاح).
- 0 screenshot حقيقي.
- لا commit git.
- الخطوة التالية بالتفصيل في نفس هذا الملف أعلاه وفي HANDOFF.md.

## إعادة التشغيل النهائية (بعد آخر تعديل، كما يشترط القسم 18/19)
اكتُشف عيب حقيقي أثناء إعادة تشغيل المجموعة كاملة: `tests/__init__.py` في
`octa_hub_api/tests` جعل pytest يستورد `octa_hub_api/__init__.py` الأب
(الذي يستورد `odoo` غير المثبَّت) → فشل تجميع الاختبار. أُصلح بحذف
`tests/__init__.py` (لم يكن لازمًا لملف اختبار مستقل) — لم يُعدَّل أي
assertion ولم يُحذف أي اختبار. أُعيد تشغيل المجموعات الأربع كاملة بعد
الإصلاح: **24/24 PASS** من جديد. تفاصيل كاملة في `docs/test-report.md`.

## مراجعة ثالثة — تركيز على البورتال (الألوان والتصميم وأكوادها)
أُعيدت قراءة `octa_hub_ui` سطرًا سطرًا. اكتُشفت وأُصلحت عيوب حقيقية:
1. `dashboard.js` كان يشاور على template باسم "octa_hub_ui.Dashboard" **غير
   موجود إطلاقًا** في المستودع (الملف لم يُنشأ من الأساس رغم ذكره في
   المانيفست) — كان سيمنع تحميل الموديول بالكامل. أُنشئ `xml/dashboard.xml`.
2. `dashboard.js` كان يستدعي `get_dashboard_metrics` على موديل
   `octa.hub.order` **وهي دالة غير موجودة** في `models/order.py`. أُضيفت
   الدالة فعليًا مع تمييز صريح بين "غير متاح" و"صفر" (القسم 10).
3. `design_tokens.scss` كانت نسخته الأولى متغيرات جذر فقط بلا أي كلاس مكوّن
   فعلي — كل الكلاسات المُشار إليها في الكود (`.o_octa_metric_card` إلخ)
   كانت بلا تنسيق. أُعيد بناء الملف بالكامل بمكوّنات فعلية، **وتحقق فعليًا
   بتجميعه بـ`sass` (npm) — نجح التجميع (6911 بايت CSS ناتج)**.
4. لم تكن هناك مكوّنات OWL قابلة لإعادة الاستخدام (StatusBadge, MetricCard,
   EmptyState, ErrorState, Timeline, SecretRevealOnce) رغم أنها مذكورة في
   كلا المصدرين — أُنشئت في `shared_components.js` + `components.xml`.
5. شاشتا قائمة الطلبات وتفاصيل الطلب (screens 06/07) لم تكونا مبنيتين
   إطلاقًا — أُنشئتا الآن (`order_list.js/xml`, `order_detail.js/xml`) مع
   رسائل دقيقة لكل حالة نقل (لا نكتب "فشل" لحالة غير معروفة، Blueprint P75).
6. تحقق تلقائي: كل مكوّن OWL مستخدم في كل template مُعرَّف فعليًا في
   `static.components` المقابل له في كل الملفات الأربعة — صفر مرجع ناقص.
7. عُرضت معاينة HTML/CSS ثابتة فعلية (عبر أداة Visualizer) لنفس الألوان
   والتخطيط، لأول مرة في هذا المشروع — إثبات بصري حقيقي للألوان (#F6F8FB،
   #2457C5، #167548، #B42318 إلخ) رغم استحالة تشغيل Odoo/متصفح فعلي هنا.
   هذه المعاينة HTML مستقلة، وليست جزءًا من موديول Odoo القابل للتثبيت.

## مراجعة رابعة — تركيز على الصلاحيات وأنواع المستخدمين والحركات (state transitions) والسكيورتي والألوان
قراءة سطر-بسطر لملفات الصلاحيات والهوية، وليس فقط فحص اتساق بنيوي. عيوب
حقيقية اكتُشفت وأُصلحت (تفاصيل كاملة في docs/security.md):
1. `active_membership_id` كان حقلاً "write-only" غير مُستخدَم في أي فحص
   صلاحية فعلي — أُصلح بقيد `_check_single_authority_per_user` جديد.
2. `has_permission()` كانت تتجاهل `membership.active` وهوية المستخدم —
   عضوية مسحوبة كانت تمنح صلاحياتها القديمة. أُصلح.
3. `organization.py::suspend()` كان يشير لدالة غير موجودة
   (`_revoke_all_sessions_for_org`) — توثيق كاذب. أُزيل، وأُضيف استدعاء
   حقيقي (NOT VERIFIED بصراحة).
4. لا حاجز كان يمنع كتابة أي حالة طلب مباشرة — أُضيف `order.py::write()`
   override يستدعي state machine حقيقي.
5. سطر ميت `self.ensure_one() if len(self) == 1 else None` (لا يفعل شيئًا
   فعليًا) — حُذف.

بُنيت 3 وحدات pure-Python جديدة، كل واحدة مُختبَرة فعليًا بمعزل عن Odoo:
- `lib/authority_context.py` (6/6 PASS) — منع اتحاد الهيئات الثلاث.
- `lib/state_machine.py` (11/11 PASS) — قواعد انتقال الحالة ("الحركات").
- `lib/ssrf_guard.py` (11/11 PASS + تحقق شبكي حقيقي إضافي ضد pypi.org
  وlocaltest.me) — كان NOT_STARTED بالكامل، أول تنفيذ فعلي له.

**الألوان**: `tools/design_check/contrast_check.py` — فحص WCAG حقيقي
لـ8 أزواج ألوان، كلها PASS (تفاصيل في docs/design-system.md، وهو ملف كان
مطلوبًا في القسم 15 منذ البداية ولم يُنشأ في أي جولة سابقة — أُنشئ الآن).

إجمالي الاختبارات الحقيقية بعد هذه الجولة: 24 (السابقة) + 28 (جديدة) = **52
اختبار PASS**، كلها أُعيد تشغيلها فعليًا بعد كل التعديلات.

**ما زال غير محلول (بصدق)**: تعدد عضوية بنفس الهيئة (نفس party_type، عملاء
مختلفين) لا يزال يُتاح رؤيته بالكامل عبر record rules الحالية (union عبر
OR) — هذا **ليس** مخالفًا لنص القسم 6 حرفيًا (الذي يتكلم عن "هيئات
مختلفة" فقط)، لكنه قرار منتج مفتوح غير محسوم (موثّق في decisions.md):
هل محاسب يخدم عميلين يرى الاثنين معًا أم واحدًا في كل مرة؟

## مراجعة خامسة — تدقيق ذاتي على إصلاحات الجولة الرابعة نفسها
بدل قراءة كود قديم فقط، راجعت الكود الذي **كتبته أنا في الجولة السابقة
مباشرة** بعين الشك. النتيجة: ثغرة SSRF حقيقية وقابلة للاستغلال في
`lib/ssrf_guard.py` (استثناء `localhost`/`127.0.0.1` كان يتفادى الفحص
بالكامل حتى مع HTTPS، وهو موصول بحقل يُدخله العميل مباشرة). أثبتّ الاستغلال
فعليًا بتشغيل استدعاء حقيقي قبل الإصلاح، ثم حذفت الاستثناء نهائيًا بعد
التأكد أنه لا حاجة فعلية له في أي مسار استدعاء حقيقي بالمشروع. كذلك
اكتشفت أن حاجز حالة الطلب المُضاف في `order.py::write()` **لا يغطي
create()** — أُضيف `create()` override منفصل يمنع البدء من حالة غير
ابتدائية. التفاصيل الكاملة والدليل في docs/security.md.

إجمالي الاختبارات الحقيقية بعد هذه الجولة: 53 (كان 52؛ +1 صافي: حُذف
اختبار كان يثبت السلوك الخطر، وأُضيف اختباران يثبتان الإصلاح). أُعيد تشغيل
كل شيء فعليًا: 53/53 PASS، py_compile نظيف، XML سليم.

## مراجعة سادسة — تدقيق مستقل صارم + فتح صور المرجع لأول مرة
1. أُعيدت قراءة الملفين من الصفر بلا اعتماد على الذاكرة، كما طُلب صراحة.
2. **اكتشاف جوهري**: 11 صورة مُضمَّنة في الـdocx لم تُفتح في أي من الجولات
   الخمس السابقة (فقط نص/جداول عبر python-docx). فُتحت جميعها الآن وعُرضت.
3. نتائج فتح الصور: تعارض ألوان حقيقي (أزرق مقابل كحلي+teal)، تعارضان
   داخليان في المرجع نفسه (عدد بطاقات النظرة العامة، نمط timeline)، نطاق
   لوحة تشغيل داخلية أضخم بكثير من المُقدَّر، تأكيد بصري مباشر أن التسويات
   المالية "قيد التطوير" (وليس تخمينًا).
4. صُحِّحت الألوان في design_tokens.scss (المجموعة الفاتحة)، مع ضبط التباين
   ليجتاز WCAG AA فعليًا (تجربة عدة قيم hex حتى النجاح: 4.90).
5. أُعيد هيكلة التوثيق بالكامل: requirements-coverage.md (جدول 7 أعمدة
   بالضبط كما طُلب)، audit-findings.md (جديد، بالشدة)، test-results.md
   (جديد، بيئة حقيقية)، RUNBOOK.md (جديد).
6. أثناء كتابة RUNBOOK اكتُشف عيب معماري إضافي: sys.path.insert يتكرر داخل
   كل نداء دالة في 4 ملفات — أُصلح لمرة واحدة عند التحميل. وأثناء هذا
   الإصلاح حدث خطأ عرضي (حذفت سطورًا مطلوبة بالخطأ في membership.py) —
   اكتُشف فورًا بإعادة القراءة وأُصلح، وتحقّقت بـpy_compile فورًا بعده.
7. أُعيد تشغيل كل الاختبارات (53) بعد كل تغيير — لا فشل واحد.

**الحالة النهائية:** 53/53 PASS، py_compile نظيف، SCSS يتجمّع، لا Odoo حقيقي
مشغّل (كما في كل الجولات). التفاصيل الكاملة موزّعة الآن على 5 ملفات
منفصلة كما طُلب بالضبط بدل ملف واحد متضخم.

## مراجعة سابعة — "جرّب كويس" حرفيًا: طرق تشغيل واختبارات لم تُجرَّب من قبل
1. **اكتشاف كبير**: جرّبت تشغيل كل مجموعات الاختبار معًا من جذر المشروع
   بأمر pytest واحد (أول شيء طبيعي يجرّبه أي حد) — فشل بـ36 خطأ من 53!
   جرّبت `--import-mode=importlib` كحل، خرّب استيرادات تانية بدل ما يصلح.
   الحل الصح: عزل كل مجموعة بعملية Python منفصلة (`tools/run_all_tests.py`).
2. اختبرت سيناريوهين جديدين في reconcile.py ماكانوش اتجربوا قبل كده:
   تكرار نفس external_id جوه نفس الدفعة (آمن)، ونفس رقم version بمحتوى
   مختلف (بيتعامل كـupdate عادي، مش conflict — قرار ضمني وثّقته دلوقتي).
3. اختبرت إجهاد أعلى من المطلوب: 200 thread بدل 20 (نجح)، و20 اتصال HTTP
   حقيقي متزامن فعليًا (مش استدعاء دوال) ضد السيرفر الحقيقي (نجح).
4. فحصت التحذيرات (deprecation warnings) عبر كل المجموعات — صفر تحذير.
5. فحصت استقلالية الاختبارات (كل ملف لوحده، ترتيب مختلف) — لا اعتماد خفي.

**الحالة النهائية: 57/57 اختبار PASS، عبر أمر تشغيل موحَّد واحد آمن الآن.**

## مراجعة ثامنة — Gate E: تنفيذ توسعة OC05 بعد مراجعة FeedUs (v1.2/v0.5)
استُلم ملفان جديدان (Phase_1-2.md v1.2 وChanges_v0_5.md — محتواهما متطابق،
الثاني نسخة مستقلة من نفس تحديث القسم 22 المُضمَّن في الأول). يضيفان 22
بند "OC05" (Gate E) من مراجعة تنافسية مباشرة لحساب FeedUs Staging.

1. أعدت التحقق من البيئة (لم يتغيّر شيء: لا Odoo/PostgreSQL/root).
2. فحصت الكود فعليًا (`grep`) لتصنيف كل بند من الـ22 قبل أي تعديل، كما
   يشترط الملف حرفيًا ("تصنيف كل بند... قبل تغييره"). النتيجة: 5 كيانات
   مطلوبة (Brand, ModifierGroup, PriceOverride, AvailabilityState,
   ExternalMapping كموديل) لم تكن موجودة إطلاقًا.
3. نفّذت 6 وحدات pure-Python جديدة مُختبَرة بالكامل لأكثر بنود P0 قابلية
   للتنفيذ بلا Odoo: price_resolution (9)، modifier_rules (9)،
   availability_state (10)، external_mapping (6)، connector_capabilities
   (6)، reliability_metrics (7) — 47 اختبار جديد، كلها PASS.
4. وصلت reliability_metrics.py فعليًا بـget_dashboard_metrics في order.py
   (كان p95 عنصرًا نائبًا None بلا حساب).
5. أضفت octa.hub.brand model + branch.brand_id (OC05-02) مع قيد تطابق tenant.
6. أعدت تشغيل كل الاختبارات (تحقق ACL، py_compile، unified runner).
7. أنشأت الملفات الجديدة المطلوبة صراحة: CHANGELOG.md، docs/migrations.md،
   docs/screen-specs.md. حدّثت requirements-coverage.md (جدول تصنيف كامل
   للـ22 بندًا)، audit-findings.md، test-results.md، architecture.md،
   security.md.

**الحالة النهائية: 104/104 اختبار PASS (كان 57). 15 من 22 بند OC05 لا تزال
NOT_STARTED بصراحة تامة — لا إسقاط صامت لأي منها.**

## مراجعة تاسعة — استكمال Gate E (متابعة "Continue")
تابعت العمل على البنود P0 المتبقية من OC05:
1. `lib/order_trace.py` (OC05-14) — فصل أحداث أمامية/عكسية، إثبات أن
   HTTP 200 لا يعني تسجيل POS. 6 اختبارات PASS.
2. `lib/reprocessing.py` (OC05-15) — معاينة أهلية، attempt_id منفصل عن
   order_id، rate limiter لكل تاجر/قناة. 7 اختبارات PASS — **إحداها تعيد
   استخدام idempotency.py الحقيقي عبر استيراد مباشر بين المجلدات**، تطبيقًا
   حرفيًا لتحذير الملف الجديد ("لا تُنشأ وظائف مكررة").
3. `lib/activation_checklist.py` (OC05-03) — 7 اختبارات PASS. **أثناء
   الربط الفعلي بالـwizard، اكتُشف bug حرج**: التقدّم كان مخزَّنًا بالكامل
   على TransientModel يُحذف تلقائيًا في Odoo — يخالف "الحفظ والاستكمال
   آمنان" مباشرة. أُصلح بنموذجين دائمين جديدين (`activation.py`)، والـwizard
   أُعيد كتابته ليشير إليهما بدل تخزين الحالة على نفسه.
4. حدّثت ACL (4 صفوف جديدة)، تحققت من الاتساق تلقائيًا (صفر فجوة)،
   py_compile نظيف، أعدت تشغيل كل شيء عبر tools/run_all_tests.py.

**الحالة النهائية: 124/124 اختبار PASS (كان 104). 12 من 22 بند OC05 لا
تزال NOT_STARTED صراحة.**

## مراجعة عاشرة — فحص تعارض العقود بين الموديولات (بدل نطاق جديد)
بدل بناء المزيد من OC05، ركّزت هذه الجولة على تماسك ما بُني عبر كل
الجولات السابقة مع بعضه — فحص صريح مطلوب في التكليف الأخير ("تعارض
الحقول أو الحالات أو العقود بين الموديولات"). النتيجة: 3 تعارضات حقيقية:

1. قدرات الموصل معرَّفة بمفردتين غير متوافقتين (`simulator.py` القديم
   مقابل `connector_capabilities.py` الجديد) — وُحِّدتا، اختبار جديد يثبت
   الإنفاذ الفعلي عبر التعريف الموحَّد.
2. أحداث دورة حياة الطلب معرَّفة بمفردتين (`order.py::event_type` Odoo
   مقابل `order_trace.py` الجديد) — لم أُعد التسمية (خطر migration)، بل
   أضفت خريطة تطابق **يتحقق منها اختبار آلي يقرأ order.py الفعلي**.
3. الداشبورد كان يحسب median/p99/حجم العينة ولا يعرضهم إطلاقًا رغم أن
   OC05-17 يشترط عرضهم صراحة — أُصلح الطرفان (backend + XML + CSS).

كذلك اكتُشفت فجوة تقريب حقيقية في `price_resolution.py` (القسم 6 يطلبها
صراحة) — أُضيف جدول دقة عملة صريح، واكتشفت حالتين حقيقيتين (KWD 3 خانات،
JPY صفر خانات) كانتا ستُكسَران بافتراض ساذج بخانتين للجميع.

**الحالة النهائية: 131/131 اختبار PASS (كان 124). صفر عيوب أمنية جديدة
هذه الجولة — التركيز بالكامل على تماسك العقود، كما طُلب.**

## مراجعة حادية عشرة — مراجعة قبول نهائية (فحص ملف-بملف)
لم يُرفَع ZIP جديد في هذه الرسالة (تحقّقت فعليًا من `/mnt/user-data/uploads/`
قبل الافتراض). اعتمدت آخر ZIP سلّمته فعليًا، فككته في مسار معزول
`/home/claude/review_pass/`، حسبت بصمته (SHA-256)، وأعدت تشغيل اختباراته
من هناك (131/131 PASS)، ثم قارنته بمجلد العمل (`diff -rq` صفر اختلاف) —
تأكيد أن ما أراجعه هو فعلًا ما سلّمته، لا افتراضًا.

أنشأت `docs/file-review-register.md` (ملف جديد مطلوب صراحة) يغطي كل ملف
كود مصدري (76 ملفًا) بحالة مراجعته ومشاكله المكتشفة. أثناء بناء هذا
السجل، أعدت قراءة ملفات لم تُفحص بعمق كافٍ من قبل:
- `api_controller.py`: لقيت `except Exception` عام يخفي أخطاءً حقيقية،
  ولا استدعاء لـ`enforce_capability` رغم بناء المنطق الكامل في Gate E.
- `invitation.py`: حقل `superseded_by_id` معرَّف ولا يُكتب أبدًا.
- `auth_controller.py`: لا مسار لاستعادة كلمة المرور رغم دعم الموديل له.
- `membership.py`/`partner_grant.py`: 5 حالات `ValueError` بدل
  `ValidationError` في قيود `@api.constrains`.

أصلحت الخمسة جميعًا، تحققت بـpy_compile + XML صالح، وأعدت تشغيل كل
الـ131 اختبارًا (لا كسر). حدّثت audit-findings.md، requirements-coverage.md،
test-results.md، وهذا الملف.

**الحالة النهائية: 131/131 اختبار PASS (بلا تغيير عددي — الإصلاحات هذه
الجولة بنيوية في كود Odoo NOT RUN). 76 ملف مصدري، كلها مراجَعة على الأقل
مرة، 2 مُعلَّمان بحاجة مراجعة أعمق مستقبلًا.**

## مراجعة ثانية عشرة — استجابة لمراجعة مستقلة خارجية (تحقق فعلي من مصدر Odoo 19)
استُلم تقرير مراجعة مستقلة خارجية حقيقي (وليس مني) — راجع مراجع فعليًا
حاول تثبيت Odoo/PostgreSQL بنفسه ووثّق فشل ذلك بأدلة (نفس القيد البيئي
المُكتشَف من الجولة الأولى)، وحدَّد 16 ملاحظة تقنية دقيقة (R01-R16) عن
توافق Odoo 19.

**الأهم: حاولت التحقق من كل ملاحظة بدل تصديقها أو رفضها بلا دليل** —
واستطعت فعليًا تنزيل مصدر Odoo 19 الحقيقي (`git clone --depth 1
--branch 19.0`، نفس عائلة الكود التي استخدمتها المراجعة الخارجية) والتحقق
المباشر. النتيجة: **12 من 16 ملاحظة مؤكَّدة 100% بدليل مصدري مباشر**:

1. `fields._tz_get` غير موجودة (R01) — أُصلحت بالاستيراد الصحيح.
2. `res.groups.category_id` غير موجود، صار `privilege_id` (R02) — أُصلح
   بإنشاء `res.groups.privilege`.
3. **`_sql_constraints` غير مدعومة فعليًا، تحذير + تجاهل صامت للقيد**
   (R03) — أخطر اكتشاف في تاريخ المشروع: القيد الأهم (منع تكرار الطلبات)
   لم يكن ليُطبَّق أبدًا فعليًا. حُوِّلت 8 قيود عبر 6 ملفات لـ`models.Constraint`.
4. 7 موديلات بلا عزل رغم ACL (R04) — أُضيفت 7 قواعد عزل.
5. `type="json"` لا يضبط حالة HTTP حقيقية (R09) — أُعيدت كتابة
   `api_controller.py` بالكامل بـ`type="http"`.
6. لا مقارنة محتوى، لا savepoint (R10) — أُصلح.
7. `str(items)`، عملة ثابتة (R11) — أُصلح.
8. **`rec.field is None` يفشل دائمًا لأن Odoo يُرجع False لا None**
   (R07) — bug دقيق ومؤكَّد بالسطر الحرفي من `Field.convert_to_record`؛
   كان يعطّل معاينة كل دعوة دائمًا. أُصلح + اختبار تراجع (4/4 PASS).
9. قبول الدعوة لا يفعّل حسابًا فعليًا (R06) — أُصلح.
10. طلب استعادة كلمة المرور يتجاهل الرمز (R08) — أُصلح جزئيًا.
11. اختبارات Odoo غير قابلة للاكتشاف (R16) — استُعيد `tests/__init__.py`
    (كنت أزلته جولة 7)، بعد التأكد أن هذا آمن مع عزل subprocess الحالي.
12. مكوّنات الواجهة غير موصولة بأي مسار تنقّل (R15) — سُجِّلت
    order_list/order_detail في actions registry (لم تكونا مسجَّلتين
    إطلاقًا)، أُضيف XML حقيقي (`ir.actions.client` + `menuitem`) مُتحقَّق
    من نمطه في مصدر Odoo الحقيقي.

متبقٍ صراحة: R05 (أمان مفتاح API)، R12 (capability تعريف مؤقت)، R13 (لا
queue/worker حقيقي) — موثَّقة، لم يتسع الوقت.

**الحالة النهائية: 135/135 اختبار PASS (كان 131). أول مرة في هذا المشروع
يُتحقَّق فيها من توافق Odoo فعليًا بمصدر حقيقي، لا معرفة نظرية فقط.**

## مراجعة ثانية عشرة — مراجعة مستقلة خارجية حقيقية + تحقق من مصدر Odoo 19
استُلم تقرير مراجعة مستقل خارجي (16 ملاحظة R01-R16)، يحمل دليل بصمة نسخة
واضحًا وسجل اختبارات فعلي (131 اختبارًا PASS من نسخته). بدل الرد على
التقرير نصيًا، **استُنسِخ مصدر Odoo 19 الحقيقي فعليًا** (git clone فرع
19.0 — الشبكة سمحت بذلك) وفُحصت كل ملاحظة حرجة مباشرة من الكود قبل أي
تعديل، كما طُلب صراحة ("تحقق من كل ملاحظة مقابل الكود ومصدر Odoo 19").

12 من 16 ملاحظة تأكدت 100% بدليل مصدري مباشر (اقتباسات حرفية من ملفات
Odoo 19 الحقيقية موثَّقة في audit-findings.md)، وأُصلحت جميعًا:
- R03 الأخطر: `_sql_constraints` مُتجاهَلة صامتًا بالكامل في Odoo 19 —
  حُوِّلت 8 قيود عبر 6 ملفات لصياغة `models.Constraint` الجديدة.
- R07: `rec.consumed_at is None` تفشل دائمًا (Odoo يحوّل None إلى False) —
  bug حرج فعليًا كان يعطّل كل عرض دعوة. أُصلح + 4 اختبارات تثبته.
- R01/R02: أخطاء توافق حقيقية (`fields._tz_get`، `res.groups.category_id`)
  كانت ستمنع تثبيت الموديول من الأساس. أُصلحا.
- R04: عزل مفقود لـ7 موديلات رغم ACL كاملة — أُضيفت 7 قواعد عزل.
- R06/R08/R09/R10/R11/R14/R16: إصلاحات وظيفية وبنيوية متعددة، كل واحد
  موثَّق بسببه ودليله في audit-findings.md.
- R15: اكتُشف أثناء الإصلاح أن order_list.js/order_detail.js لم يكونا
  حتى مُسجَّلين في JS action registry (فقط dashboard.js كان مُسجَّلاً) —
  أُصلح الثلاثة معًا + XML actions/menu جديد.

3 ملاحظات (R05, R12, R15) سُدَّت جزئيًا فقط، مذكورة صراحة كـNOT VERIFIED.
1 ملاحظة (R13، لا queue/worker حقيقي) لم تُعالَج إطلاقًا — أكبر فجوة
بنيوية متبقية.

أُعيد تشغيل كل الاختبارات بعد كل دفعة إصلاحات (py_compile، XML، JS syntax،
SCSS، YAML، اتساق ACL، tools/run_all_tests.py) — صفر كسر في أي خطوة.

**الحالة النهائية: 135/135 اختبار PASS (كان 131).**

## مراجعة ثالثة عشرة — رد فعلي على مراجعة V2 مستقلة (إصلاح، لا رد نصي)
استُلمت مراجعة V2 قدّمت أدلة تنفيذية حقيقية (تشغيل runner فعلي 135 PASS،
واستخراج AST فعلي لـreceive_order_event وتشغيله بمحاكيات لإثبات مسار نجاح
كاذب). أول مرة أراجع فيها عمل مراجعة مستقلة أخرى (لا مصدر Odoo مباشرة) —
تحققت كل ملاحظة حرجة بدليل مستقل قبل القبول.

1. تحققت من اختلاف بصمة الأرشيف: فككت الملف المرفوع فعليًا، قارنته
   (diff -rq) بمجلد عملي — صفر اختلاف محتوى. أضفت SOURCE_MANIFEST.sha256
   (بصمة لكل ملف على حدة) يحل هذا نهائيًا.
2. أصلحت R16 فعليًا (groups_id → group_ids، مُتحقَّق من مصدر Odoo 19).
3. أصلحت "نجاح الاستقبال الوهمي" جذريًا: ضيّقت الاستثناء لـUniqueViolation
   تحديدًا، أضفت تحقق اسم القيد الفعلي، ورفضت الادعاء بالنجاح بلا سجل
   مطابق (500 بدل 200 مزيَّف). اكتشفت أثناء ذلك أن `.diag` في psycopg2 هو
   slot للقراءة فقط على مستوى C — فصلت منطق القرار لوحدة قابلة للاختبار
   الكامل (duplicate_resolution.py، 5/5 PASS) بدل التظاهر باختبار ما لا
   يمكن اختباره.
4. أصلحت عزل تفويض الشريك (expires_at/branch_ids) — اكتشفت أن ir.rule في
   Odoo 19 لا يملك datetime في سياق تقييمه إطلاقًا (تحقَّقت من المصدر)،
   فبنيت حقل is_currently_valid مخزَّن بدلًا من محاولة حساب مستحيل.
5. صححت خطأ توثيقي حرج كنت قد كتبته بنفسي سابقًا عن دلالات OR/AND بين
   قواعد Odoo — تحققت من ir_rule.py الحقيقي وأثبتت أن المراجعة V2 كانت
   محقة، وسجّلت الخطر الحقيقي المتبقي (اتحاد أدوار التاجر) صراحة بدل إخفائه.
6. أصلحت قفل استهلاك الدعوة (lock_for_update() حقيقية من مصدر Odoo 19) +
   تقييد purpose + فحص تعليق الحساب/العميل.
7. أصلحت البريد (mail.mail حقيقي بدل log) وR05 (دالة تجزئة مفتاح API لم
   تكن موجودة إطلاقًا، أُنشئت).
8. أصلحت عدم تطابق شكل بيانات Timeline في order_detail.js — كانت ستعرض
   عناصر فارغة تمامًا (IDs خام بدل سجلات محوَّلة).
9. اكتشفت وأصلحت __init__.py قديم منسي في octa_hub_api/lib (نفس علة
   F-TEST-01 من الجولة السابعة) أثناء إضافة اختبارات جديدة هناك.

**الحالة النهائية: 145/145 اختبار PASS (كان 135). لم يُعالَج: R12، R13 (لا
queue/worker حقيقي — أكبر فجوة متبقية)، عمق R14، اتحاد أدوار التاجر.**

## مراجعة رابعة عشرة — إغلاق الفجوات الأربع المتبقية (طلب صريح: "خلصني منها")
عالجت الفجوات الأربع الموثَّقة في نهاية الجولة السابقة بدرجات مختلفة:

1. **اتحاد أدوار التاجر عبر OR** — أُغلق بالكامل. وسّعت
   `authority_context.py` بدالة تمنع تعدد الأدوار داخل نفس الهيئة (لا
   الهيئات الثلاث فقط)، مع إبقاء صريح على السماح بنفس الدور عبر عملاء
   مختلفين. 4 اختبارات جديدة، وُصلت بقيد membership.py الموجود.
2. **R12 (تعريف قدرة مؤقت)** — أُغلق بالكامل. بنيت نموذجي Odoo دائمين
   (connector.definition + capability.line)، بيانات حقيقية، وربط فعلي في
   الـcontroller بدل الافتراض المؤقت. اكتشفت أثناء ذلك أن octa_hub_api
   لم يكن لديه ملف ACL إطلاقًا منذ إنشائه — أنشأته.
3. **R13 (لا queue/worker)** — أكبر فجوة، عالجتها جزئيًا بجدية: نمط outbox
   كامل مُختبَر (8/8، منها تزامن 20 thread حقيقي يثبت عدم أخذ نفس العنصر
   مرتين)، نموذج Odoo دائم يستخدم lock_for_update() الحقيقية، cron حقيقي.
   وصلت الاستقبال بالإدراج الفعلي. **اكتشفت وأصلحت في نفسي**: مسودتي
   الأولى للربط كانت تبتلع فشل الإدراج بصمت (try/except) — أدركت أن هذا
   يترك الطلب محفوظًا بلا طابور للأبد، فأزلت المعالجة الصامتة لصالح فشل
   ذري صريح (transactional outbox صحيح). الإرسال الفعلي لنظام POS من
   داخل العامل يبقى NOT_STARTED صراحة — لم أدّعِ إغلاقًا كاملًا.
4. **R14 عمق** — أضفت حقلي توقيت صريحين يُسجَّلان عند لحظة الانتقال
   الفعلية في write() (حيث حاجز state_machine موجود أصلًا)، بدل الاشتقاق
   من write_date/create_date العامين.

أعدت تشغيل كل الاختبارات بعد كل خطوة (py_compile شامل، اتساق ACL لكل من
octa_hub_core وocta_hub_api معًا، XML، unified runner) — صفر كسر.

**الحالة النهائية: 157/157 اختبار PASS (كان 145). 3 من 4 فجوات CLOSED
بالكامل منطقيًا، 1 (R13) PARTIAL بصدق — التكامل الفعلي مع موصل حقيقي
لا يزال مفقودًا.**

## مراجعة خامسة عشرة — تدقيق ذاتي على كود الجولة السابقة (طلب: تست وحلل تاني)
بدل نطاق جديد، راجعت بعناية أكبر ما كتبته أنا بنفسي في الجولة السابقة
مباشرة (outbox_item.py، connector_definition.py، branch.py). لقيت مشكلتين
حقيقيتين:

1. `connector_definition_id` (من R12) بلا أي ربط تلقائي — أي اتصال جديد
   حقيقي كان سيُرفَض له كل طلب فورًا. أضفت create() override بربط تلقائي
   ذكي (لا يخترع رابطًا لو مفيش تعريف مطابق).
2. **اكتشاف أهم**: `claim_batch()` في outbox_item.py كانت تستخدم
   `lock_for_update()` — رجعت لمصدر Odoo 19 بعناية أكبر ولقيت إنها ترفع
   استثناء **للدفعة كاملة** لو عنصر واحد بس كان مقفول من عامل تاني — عكس
   تمامًا السلوك المطلوب لطابور (يجب أخذ المتاح وتجاهل المشغول). أصلحتها
   بـtry_lock_for_update() الصحيحة فعليًا لهذا النمط. تأكدت إن نفس الدالة
   القديمة (lock_for_update) صح تمامًا في invitation.py (سجل واحد، مش دفعة)
   — نفس الدالة ممكن تبقى صح في سياق وغلط في سياق تاني.

أعدت تشغيل كل شيء (py_compile شامل + unified runner) بعد كل تعديل — صفر
كسر. **157/157 اختبار PASS (بلا تغيير عددي، إصلاحات بنيوية في كود NOT RUN).**

## مراجعة سادسة عشرة — مراجعة بوابة هنقرستيشن (نطاق جديد بترتيب مطلوب)
بنيت جدول تصنيف صادق أولًا (docs/hungerstation-gap-review.md) قبل أي
تعديل، كما طُلب صراحة. نفّذت جزءًا من الترتيب المطلوب:

1. **storefront (القسم 1)**: نموذج جديد يفصل الفرع الفعلي عن البراند
   المعروض — يدعم أكثر من براند لنفس الموقع (مطبخ سحابي). اكتشفت أثناء
   البناء أن القيد الفريد القديم كان سيمنع بالضبط السيناريو المطلوب —
   أصلحته فورًا. بيانات اختبار حقيقية تثبت السيناريو (فرع واحد، براندان).
2. **تصحيح حالة بعد نهائية (القسم 3.3)**: مسار منفصل تمامًا عن الانتقال
   العادي، يتطلب سببًا موثَّقًا، يمنع تكرار أثر POS بنيويًا. 6 اختبارات
   جديدة، منها اختبار يثبت أن المسار العادي لم يتأثر إطلاقًا.
3. **مواعيد التشغيل (القسم 5)**: نطاق جديد بالكامل — جدول أسبوعي، عبور
   منتصف الليل (بتحقق تناسق صريح)، جداول استثنائية، فصل ثلاث حالات
   (مغلق بالجدول/متوقف أثناء التشغيل/غير معروف)، نسبة توقف داخل ساعات
   التشغيل فقط. 9 اختبارات جديدة.
4. **إعادة فحص صلاحيات المستندات (القسم 6)**: طبّقت السيناريو المطلوب
   حرفيًا في شروط التسليم (مدير فرع يحاول تنزيل تقرير متعدد الفروع)،
   وفحصين مستقلين فعليًا (توليد + تنزيل، لا فحص واحد مُعاد استخدامه).
   5 اختبارات جديدة.

أعدت التحقق الشامل بعد كل خطوة (py_compile، XML، ترتيب مراجع demo_data،
اتساق ACL، unified runner) — صفر كسر.

**الحالة النهائية: 177/177 اختبار PASS (كان 157). أقسام 2 (كموديل Odoo)،
2.5، 2.6، 3.4، 4 (الموسَّع)، 7 لم تُعالَج هذه الجولة — مذكورة صراحة في
docs/hungerstation-gap-review.md.**

## مراجعة سابعة عشرة — إغلاق أولوية القسم 2 (طلب متابعة: "لو في حاجة ناقصة نعملها")
نفّذت الأولوية القصوى المسجَّلة آخر جولة: نماذج Odoo فعلية للمنيو. بنيت
4 نماذج (catalog.item، modifier.group، modifier، price.override) كلها
مرتبطة بـstorefront (لا فرع مباشرة). استدعيت المنطق المُختبَر مسبقًا
(price_resolution.py، modifier_rules.py) بدل إعادة كتابته. أضفت بيانات
اختبار حقيقية تثبت السيناريوهات (مجموعة إضافات مرتبطة بصنفين، سعر مختلف
حسب القناة). تعمَّدت عدم إضافة قيد تفرّد على اسم مجموعة الإضافات (القسم 2
ينص صراحة على منع هذا). أضفت nutrition_info/allergen_info كحقلي نص حر
موثَّق فيهما صراحة عدم التوليد الآلي.

تحققت أيضًا من `base.SAR` (كان NOT VERIFIED منذ جولات) ضد مصدر Odoo 19
الحقيقي المتاح — مؤكَّد حقيقي.

أعدت التحقق الشامل (py_compile، XML، ترتيب مراجع، ACL، unified runner) —
صفر كسر. **177/177 اختبار PASS (بلا تغيير عددي — إضافة بنيوية فقط).**

## مراجعة ثامنة عشرة — إصلاحات مستهدفة (طلب صريح: أوقف نطاقًا جديدًا)
طلب واضح ومحدد: أصلح النجاح الوهمي في العامل، allow_zero، الأسعار
السالبة، هوية مجموعات الإضافات ومنع تكرارها، قيود اتساق التاجر، وأضف
اختبارات Odoo فعلية — مع تمييز صريح بين المكتوب والمختبر.

1. **النجاح الوهمي (الأهم)**: `run_worker_batch` كانت تسجّل done بلا أي
   إرسال حقيقي. أعدت تصميم lib/outbox.py بالكامل: عقد ثلاثي الحالة، حالة
   awaiting_confirmation جديدة، دورة استعلام منفصلة تمامًا عن الإرسال
   (لا تقبل dispatch_fn إطلاقًا — تحقق آلي من التوقيع). كتبت اختبار تكامل
   حقيقي فعلي ضد خادم mock_pos الحقيقي — لقيت فيه غلطتين بنفسي أثناء
   الكتابة (افتراض دالة مستوردة بينما هي HTTP endpoint فعلي، ومزج توقيت
   حقيقي بوهمي) وصلحتهما قبل الاعتماد. في طبقة Odoo: dispatch_fn/query_fn
   بقيا إلزاميين بلا افتراضي — استدعاء بلا محوّل حقيقي يفشل فورًا بوضوح
   بدل نجاح وهمي. عطّلت الـcron صراحة لعدم وجود محوّل حقيقي بعد.
2. **allow_zero**: كانت تُسقَط صامتًا عند بناء PriceOverride — أصلحتها.
3. **أسعار سالبة**: أضفت 3 قيود جديدة (صنف، تخصيص سعر، إضافة).
4. **هوية مجموعات الإضافات**: أضفت external_id لمجموعة الإضافات نفسها
   (لم يكن لها هوية مستقرة إطلاقًا)، وقيد تفرّد فعلي لـmodifier.external_id
   (كان موجودًا بلا قيد — نفس علة "حقل بلا وظيفة" من جولات سابقة). ضبطت
   النطاق الصحيح لكل قيد (منظمة للمجموعة، مجموعة للخيار الفردي).
5. **اتساق التاجر**: قيد جديد يمنع ربط مجموعة إضافات بصنف من منظمة مختلفة.
6. **اختبارات Odoo فعلية**: 18 اختبار TransactionCase جديد
   (test_catalog.py) يغطي الحفظ والقيود والعلاقات والصلاحيات — كلها
   NOT RUN صراحة، لا ادعاء بخلاف ذلك.

أعدت التحقق الشامل (py_compile، XML، ACL، unified runner ×2 للاستقرار) —
لاحظت فشلًا عابرًا لمرة واحدة في mock_pos (تزامن/توقيت نظام على الأرجح)،
4 تشغيلات لاحقة نظيفة تمامًا. سجّلت هذا بصدق بدل إخفائه.

**الحالة النهائية: 183/183 اختبار pytest PASS (كان 177). 18 اختبار Odoo
مكتوب إضافي (NOT RUN).**

## مراجعة تاسعة عشرة — إكمال محوِّل حقيقي + تدقيق ذاتي (طلب: "كمل")
راجعت أولًا قيود الجولة السابقة بعناية أكبر — تحققت من مصدر Odoo 19 أن
NULL في حقل Char فارغ لا يتعارض مع NULL آخر في قيد UNIQUE (فرضية خطر
كانت تستحق التحقق، أُثبتت سليمة بدليل مصدري، لا افتراضًا).

أكملت النقص المتبقي من إصلاح النجاح الوهمي: بنيت محوِّل حقيقي فعلي
(mock_pos_adapter.py) يتصل بخادم mock_pos عبر HTTP حقيقي، بعقد يطابق
outbox_item.py حرفيًا. 6 اختبارات جديدة كلها ضد خادم حقيقي فعليًا، منها
دليل تكامل شامل من الإدراج حتى done. لقيت غلطة في نفسي أثناء كتابة
الاختبار الشامل (تحميل وحدة يدويًا بلا تسجيلها في sys.modules) — اكتُشفت
بالتشغيل الفعلي، لا بالقراءة، وصُلحت فورًا.

حدّثت تعليق cron الطابور ليعكس الواقع الجديد بدقة: محوِّل حقيقي موجود
الآن، لكن الـcron يبقى معطَّلًا عمدًا لأن الخادم المستهدف أداة اختبار
تُشغَّل يدويًا، لا خدمة إنتاج فعلية.

أعدت التحقق الشامل 3 مرات متتالية — استقرار كامل، صفر تذبذب هذه المرة.

**الحالة النهائية: 189/189 اختبار PASS (كان 183).**

## مراجعة عشرون — أول تشغيل حقيقي فعلي على Odoo 19 + PostgreSQL (طلب: "نرفع المديول ونجرب")
اكتشفت أن البيئة تعمل فعليًا كـroot (whoami → root) — افتراض "لا صلاحيات
جذر" عبر 19 جولة سابقة كان خطأً؛ sudo: not found كان يعني غياب أداة sudo
فقط. ثبّتت PostgreSQL 16 فعليًا (apt-get install نجح كاملاً)، ثبّتت كل
اعتماديات Odoo 19 الحقيقية (pip install -r requirements.txt بعد تثبيت
libpq-dev/libldap2-dev/libsasl2-dev). استخدمت نسخة Odoo 19 الحقيقية
المُستنسَخة مسبقًا من GitHub.

**المحاولة الأولى** (octa_hub_core فقط): نجحت التثبيت! لكن كشفت فورًا
DeprecationWarning حقيقي (type=json في auth_controller.py لم أُصلحه رغم
إصلاح نفس النمط سابقًا في ملف آخر) وتحذير ACL مفقود لموديل activation.wizard.

**المحاولة الثانية** (كل الأربع موديولات معًا): فشلت بخطأين حقيقيين:
octa_hub_connector_demo رُفض بالكامل (لا __manifest__.py منذ الجولة
الأولى!)، وخطأ XML حقيقي (مسافات بادئة من التفاف سطر تسربت داخل xmlid
في groups=). أصلحت الاثنين.

**المحاولة الثالثة**: نجح التثبيت الكامل! شغّلت --test-enable لأول مرة
على الاختبارات الحقيقية المكتوبة عبر جولات كثيرة سابقة (29 اختبار). 12
منها فشل فعليًا — تحققت من كل واحد بعمق:
- 4 فشلت بـAttributeError: 'list' object has no attribute 'get' — تتبعت
  السبب لـinvitation.py::create() المُزيَّنة بـ@api.model قديم بدل
  @api.model_create_multi — نظام الدعوات بأكمله كان معطوبًا كليًا منذ
  الجولة الأولى، بحث شامل أكد أن كل الـcreate الأخرى صحيحة، هذا معزول.
- 1 فشل بـAccessError على قراءة الفروع — تتبعت السبب لاكتشاف **الأهم في
  هذه الجولة كلها**: لا ربط كان موجودًا إطلاقًا بين octa.hub.membership
  ومجموعات res.groups الحقيقية! كل التوثيق عبر 19 جولة افترض أن إنشاء
  عضوية يمنح صلاحية فعلية — لم يكن يفعل ذلك أبدًا. بنيت مزامنة كاملة
  (create/write/unlink) مع إعادة حساب من الصفر تضمن أن إلغاء عضوية يزيل
  الصلاحية الفعلية فعليًا.
- 2 فشلا بخطأ NotNullViolation — اختباران قديمان يستخدمان branch_id
  مباشرة على connection، أصبح حقلاً related منذ إعادة هيكلة storefront
  ولم يُحدَّث الاختباران. أصلحتهما.
- 3 فشلت بتوقع ValidationError خاطئ — تحققت من مصدر Odoo 19 أن
  psycopg2.IntegrityError الخام هو ما يصل فعليًا عند create() مباشر
  (لا تحويل تلقائي لـValidationError كما افترضت). أصلحت التوقعات + حميتها
  بـcr.savepoint() (نفس نمط سابق مُتحقَّق).
- 2 فشلا بـAccessError غير متوقع — اكتشفت أن setUp في test_invitation.py
  كان يمنح group_ids مباشرة بدل إنشاء عضوية فعلية، فرفضها فحص invitation.py
  الأمني الصحيح (سجل عضوية حقيقي، لا مجموعة ممنوحة يدويًا) — الكود
  المُختبَر كان صحيحًا وآمنًا، الاختبار كان يحاكي إعدادًا غير واقعي.

**المحاولة الرابعة (النهائية)**: 0 failed, 0 error(s) of 29 tests. تثبيت
نظيف كامل بصفر تحذيرات. أعدت تشغيل مجموعة pytest المعزولة (189/189 لا
تغيير). حدّثت RUNBOOK.md بتعليمات تثبيت حقيقية مُتحقَّقة بالكامل.

**الحالة النهائية: 189 pytest معزول + 29/29 Odoo TransactionCase حقيقي
PASS لأول مرة. 4/4 موديولات مُثبَّتة فعليًا. 9 عيوب حقيقية اكتُشفت وأُصلحت،
لم تكتشفها أي مراجعة سابقة عبر 19 جولة (بما فيها مراجعتان مستقلتان
خارجيتان).**

## مراجعة واحدة وعشرون — أول عطل حقيقي مُبلَّغ من نشر فعلي (Odoo.sh)
المستخدم رفع صورة شاشة حقيقية من بيئة dev.odoo.com فعلية: TypeError:
v1.onAction is not a function عند فتح شاشة النظرة العامة. تتبعت السبب
بدقة: components.xml::EmptyState كان يعرض زرًا ويربطه بـprops.onAction()
بمجرد وجود actionLabel، بلا التحقق من تمرير onAction فعليًا — والاستدعاء
الوحيد لهذا المكوّن بـactionLabel (في dashboard.xml) كان يمرر actionLabel
فقط. نقرة الزر = استدعاء undefined() = بالظبط رسالة الخطأ المُبلَّغة حرفيًا.

أصلحت إصلاحين: تحصين بنيوي في المكوّن (الزر لا يظهر إلا لو onAction
موجودة فعليًا، يمنع الفئة كاملة لا موضعًا واحدًا)، وسلوك حقيقي صادق للزر
نفسه (يعيد فحص البيانات فعليًا بدل وعد بفعل غير موجود، ونص الزر يطابق
سلوكه الحقيقي بدقة).

لقيت غلطتين في نفسي أثناء كتابة الإصلاح نفسه: استخدمت "and" بايثون بدل
"&&" جافاسكريبت (QWeb بيتقيّم كجافاسكريبت) — تحققت من النمط الفعلي
الموجود في نفس الملف وصححتها. وكتبت مرجعًا مخترعًا "component." بدل النمط
الصحيح "this." المستخدم في كل الملفات الأخرى — تحققت وصححتها قبل الاعتماد.

حاولت تثبيت متصفح headless (Chromium) للتحقق البصري الكامل بنقرة فعلية —
غير متاح فعليًا (حزمة apt مجرد stub لـsnap غير مفعَّل، وPlaywright يحتاج
تنزيل ثنائيات من خارج نطاقات الشبكة المسموحة). سجّلت هذا الحد بصدق بدل
الادعاء بتحقق كامل لم يحدث.

أعدت التثبيت والاختبار الكامل على Odoo 19 + PostgreSQL حقيقيين بعد
الإصلاح — 0 failed, 0 error(s) of 29 tests، بلا تغيير. أعدت pytest
المعزول أيضًا — 135/135 (189 إجمالي) بلا تغيير.

**الحالة النهائية: عطل واجهة حقيقي واحد مُصلَح بدليل تتبع دقيق + تحقق
شامل عدا نقرة متصفح فعلية (قيد بيئي موثَّق).**
