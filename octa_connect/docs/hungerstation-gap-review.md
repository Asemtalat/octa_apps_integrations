# مراجعة مقابل بوابة هنقرستيشن — تصنيف قبل التنفيذ

كل بند صُنِّف بفحص الكود الفعلي (`grep`/قراءة مباشرة) قبل أي تعديل، كما
يُشترَط. "ظهور وظيفة في بوابة هنقرستيشن لا يعني أنها متاحة عبر API" —
كل بند "يتوقف على API خارجي" مُعلَّم صراحة كذلك، لا كـ"ناقص" عادي.

| # | البند | الحالة | الملفات | دليل الاختبار |
|---|---|---|---|---|
| 1.1 | فصل تاجر/براند/موقع | جزئي | `models/organization.py::OctaHubBrand`, `models/branch.py::brand_id` | لا اختبار Odoo يغطي هذا تحديدًا |
| 1.2 | أكثر من براند/مطعم افتراضي بموقع واحد | **ناقص** | `branch_id.brand_id` علاقة Many2one **واحد لواحد** — لا تدعم تعدد براندات لنفس الموقع الفعلي | — |
| 1.3 | متجر تطبيق بمعرّف/ربط/أسعار/توفر خاص + وجهة POS صحيحة | جزئي | `models/branch.py::OctaHubConnection` (معرّف واحد فقط)، لا نموذج "متجر تطبيق" منفصل عن "اتصال" | — |
| 1.4 | لا تجاوز عزل التجار/الفروع | موجود ومختبر (للموجود حاليًا) | `security/record_rules.xml` | اختبارات Odoo مكتوبة NOT RUN؛ منطق معزول مُختبَر |
| 2.1 | ترجمة/صور/SKU/معرّفات خارجية | جزئي (الآن كموديل Odoo فعلي أيضًا) | `models/catalog.py::OctaHubCatalogItem` (name_ar/name_en/sku/external_id/image_url) + `tools/catalog_reconcile/reconcile.py` | py_compile/ACL/XML نظيفة؛ image_url رابط لا Binary حقيقي بعد |
| 2.2 | أسعار مستقلة فرع/تطبيق بلا كتابة فوق الأساس | موجود ومختبر (منطق معزول) | `lib/price_resolution.py` | 13/13 PASS |
| 2.3 | مجموعات إضافات قابلة للربط بعدة أصناف، لا الاسم كمفتاح | موجود ومختبر (منطق معزول)، **ناقص كموديل Odoo** | `lib/modifier_rules.py` (منطق فقط، لا `octa.hub.modifier.group` كموديل Odoo فعلي مُرتبَط بمنتج) | 9/9 PASS للمنطق؛ لا نموذج Odoo |
| 2.4 | حدود اختيار/إلزامية/كمية حسب قدرة التطبيق | موجود ومختبر (منطق معزول) | `lib/modifier_rules.py::channel_supports_repeat_or_reject` | ضمن 9/9 أعلاه |
| 2.5 | معلومات غذائية/حساسية من التاجر فقط، بلا توليد | **ناقص** | لا حقل واحد لهذا في أي نموذج | — |
| 2.6 | معاينة فروق قبل المزامنة + نتيجة نشر لكل متجر | **ناقص** | لا `PublishJob`/`PublishTarget` (مذكور NOT_STARTED منذ جولات سابقة) | — |
| 3.1 | سجل أحداث لا يمحو التاريخ (مصدر/معرّف/توقيت مصدر/استلام) | موجود جزئيًا | `models/order.py::OctaHubOrderEvent` (event_type/sequence/source_timestamp/received_at) | لا اختبار Odoo مباشر |
| 3.2 | تكرار/تأخر/خروج عن الترتيب | موجود ومختبر (منطق معزول) | `lib/idempotency.py` | 8/8 PASS |
| 3.3 | تصحيح حالة بعد حالة نهائية عند حدث موثوق | **ناقص، بل معاكِس للتصميم الحالي** | `lib/state_machine.py::FINAL_COMMERCIAL_STATES` تمنع **أي** خروج من الحالة النهائية إطلاقًا — لا مسار "تصحيح موثوق" مميَّز عن "تحديث قديم" | 11/11 PASS لكن **للسلوك المعاكس للمطلوب الجديد** |
| 3.4 | سبب إلغاء أصلي + تصنيف + جهة مسؤولة بعرض عربي | **ناقص** | لا حقل لهذا في `order.py` | — |
| 3.5 | فصل حالة تجارية عن نقل/تسليم تقني | موجود ومختبر | `models/order.py` (transport_state/commercial_state/payment_state محاور منفصلة) | 11/11 PASS (state_machine) |
| 4 | مؤشرات سرعة مستقلة لكل مرحلة (استقبال→حفظ→طابور→POS→قبول آلي/بشري→رجوع→تحضير→مندوب) | جزئي جدًا | `models/order.py::dispatch_started_at/registered_confirmed_at` (مرحلتان فقط من ~8)، `lib/reliability_metrics.py` (median/p95/p99 عام) | 7/7 PASS للحساب العام؛ لا حقول للمراحل الإضافية |
| 5 | ساعات عمل + عبور منتصف ليل + جداول استثنائية + مغلق/متوقف/غير معروف + نسبة توقف | **ناقص بالكامل** | لا شيء إطلاقًا | — |
| 6.1 | صلاحيات على مستوى الخادم (تقارير/تنزيلات/مرفقات/مهام خلفية) | جزئي | `security/record_rules.xml` يغطي نماذج البيانات الأساسية فقط، **لا تقارير/مرفقات/مهام خلفية** (فجوة F-ISO-01 معروفة من جولات سابقة) | — |
| 6.2 | مستند متعدد الفروع لا يتاح لمدير فرع بمجرد اختيار فرعه | **ناقص** | لا نظام تقارير فعلي أصلًا | — |
| 6.3 | إعادة فحص الصلاحيات عند التوليد وعند التنزيل | **ناقص** | نفس السبب | — |
| 6.4 | إنشاء المستخدمين فقط عبر أوكتاتيك مخوَّل | موجود ومختبر | `models/invitation.py::create()` | اختبار Odoo مكتوب NOT RUN |
| 7 | تقرير طلب تفصيلي + فلاتر + فصل قيمة/خصم/رسوم/تسوية + تعريف مؤشر وتغطية بيانات | **ناقص بالكامل كواجهة/نموذج** | `order_detail.js` يعرض أساسيات فقط، لا فصل مالي، لا فلاتر متعددة | — |
| 8 | ميزات مشروطة بتوفر البيانات، لا بيانات وهمية للتقييمات/التعويضات/الإعلانات | موجود ومختبر (نمط عام) | `lib/connector_capabilities.py` (`unsupported`/`not_verified`) — **لكن لا حقول فعلية بعد للتقييمات/التعويضات تحديدًا لتطبيق النمط عليها** | 6/6 PASS للنمط العام |

## ملخص رقمي صادق
من ~26 بندًا فرعيًا: **6 موجودة ومختبرة** (منطق معزول أو نموذج Odoo)،
**7 جزئية**، **11 ناقصة بالكامل**، **1 معاكس فعليًا للتصميم الحالي** (3.3).
**صفر بند "يتوقف على API خارجي"** حتى الآن — لأن معظم النقص حاليًا بنيوي
(نماذج ومنطق لم يُبنَ)، لا تكامل فعلي مع تطبيق حقيقي كان لينكشف هذا القيد.
