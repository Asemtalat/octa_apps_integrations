# سجل تغطية المتطلبات — Octa Connect

**تنبيه صدق أولوي (مراجعة سادسة):** الجولات الخمس السابقة استخرجت نص وجداول
`Octa_Connect_Product_Blueprint_AR_v0_4_RTL.docx` عبر `python-docx` فقط، **ولم
تُفتح صور الشاشات الـ11 المُضمَّنة داخل الملف حتى هذه الجولة**. بعد فتحها
وعرضها فعليًا الآن، تبيّن أن الألوان والتفاصيل البصرية الحقيقية للشاشات
تختلف جوهريًا عمّا افتُرض سابقًا (تفاصيل في القسم الأخير من هذا الملف
وفي `docs/audit-findings.md#F-VISUAL-01`). هذا يعني: كل تقييمات "الواجهة"
في الجولات 1-5 كانت مبنية على وصف نصي غير مكتمل، لا على مطابقة بصرية فعلية.

**المصادر:** `Octa_Connect_Claude_Phase_1.md` (v1.1) و
`Octa_Connect_Product_Blueprint_AR_v0_4_RTL.docx` (v0.3، متضمنًا 11 صورة
مُضمَّنة رُوجعت الآن فعليًا لأول مرة) + تعديلات المحادثة.

**فصل الحالة عن الدليل (كما هو مطلوب):**
- **حالة التنفيذ** = هل الكود/الملف موجود؟ (`WRITTEN` / `PARTIAL` / `NOT_STARTED` / `DEFERRED`)
- **دليل الاختبار** = هل شُغِّل فعليًا وما نتيجته؟ (`PASS n/n` مع اسم الاختبار، أو `NOT RUN` صراحة، أو `N/A — لا اختبار مناسب بعد`)
وجود الكود لا يعني نجاح الدليل، والعكس غير ممكن منطقيًا. لا نسبة اكتمال تقديرية في هذا الملف.

---

## البوابة A — الأساس والهوية والصلاحيات

| المعرّف | المرجع والقسم | المتطلب | مكان تنفيذه | حالة التنفيذ | دليل الاختبار | النقص والإجراء |
|---|---|---|---|---|---|---|
| A1 | Phase_1 §6 | فصل Organization عن res.company | `models/organization.py` | WRITTEN | NOT RUN (لا Odoo) | يحتاج تثبيت Odoo 19 فعلي — انظر RUNBOOK.md |
| A2 | Phase_1 §6 | Branch مستقل تحت tenant | `models/branch.py` | WRITTEN | NOT RUN | نفسه |
| A3 | Phase_1 §6 | لا اتحاد صلاحيات هيئات مختلفة | `models/membership.py::_check_single_authority_per_user` | WRITTEN (أُصلح جولة 4 بعد اكتشاف أن `active_membership_id` كان غير مُستخدَم فعليًا) | `lib/authority_context.py` **PASS 6/6** (منطق معزول)؛ القيد الفعلي في Odoo NOT RUN | يحتاج اختبار Odoo فعلي يثبت رفض إنشاء عضوية ثانية بهيئة مختلفة |
| A4 | Phase_1 §6 | PartnerGrant محدد بعميل/فروع/أفعال/مدة | `models/partner_grant.py` | WRITTEN | NOT RUN | — |
| A5 | Phase_1 §6 | فصل read/write/export/publish/rotate/disconnect/reprocess | `models/role_permission.py::has_permission` | WRITTEN (أُصلح جولة 4: كان يتجاهل `membership.active` وهوية المستخدم) | NOT RUN | — |
| A6 | Phase_1 §6 | فصل أدوار أوكتاتيك الداخلية | `security/security_groups.xml` (5 مجموعات) | WRITTEN | NOT RUN | مجموعتا `octatech_finance`/`octatech_integration` بلا أي ACL مرتبط بعد (لا نماذج لهما) |
| A7 | Phase_1 §6/7 | إنشاء الحسابات فقط لمخوّل أوكتاتيك | `models/invitation.py::create()` + ACL `perm_create=0` لغير أوكتاتيك | WRITTEN (طبقتان: ACL + Python check) | NOT RUN | — |
| A8 | Phase_1 §7 | دورة حساب pending→verified→active→suspended→revoked | `organization.py` (state)، `invitation.py` | WRITTEN | NOT RUN | لا حالة "revoked" منفصلة عن "suspended" فعليًا على مستوى العضوية نفسها بعد |
| A9 | Phase_1 §7 | رابط تفعيل عشوائي آمن محدود العمر لمرة واحدة | `invitation.py` (`secrets.token_urlsafe(32)`, hash فقط) | WRITTEN | NOT RUN | — |
| A10 | Phase_1 §7 | إعادة الإصدار تُبطل السابق فورًا | `invitation.py::issue()` | WRITTEN | NOT RUN | — |
| A11 | Phase_1 §7/8 | بصمة الرمز فقط، لا تسريب في اللوج | `invitation.py::_hash_token` | WRITTEN | لا اختبار آلي يفحص محتوى اللوج فعليًا | فجوة: لا أداة تتحقق آليًا أن `_logger.info` لا يطبع أسرارًا — فحص يدوي فقط |
| A12 | Phase_1 §7 | كلمة المرور يضعها المستخدم، لا تُرسل بالبريد | `controllers/auth_controller.py::invite_accept` | WRITTEN | NOT RUN | ربط `res.users.create` الفعلي NOT VERIFIED (انظر decisions.md) |
| A13 | Phase_1 §7 | دخول: كلمة مرور ثم كود بريد؛ TOTP للحسابات الحساسة | — | **NOT_STARTED** | — | يحتاج تحقق مباشر من `auth_totp` على Odoo 19 حقيقي — مستحيل بدون تثبيت فعلي |
| A14 | Phase_1 §7/14 | جلسة ما قبل التحقق لا تصل لبيانات (مسارات مخصصة وعامة) | `auth_controller.py::pre_auth_probe` (مخصص فقط) | PARTIAL | مسار مخصص واحد فقط؛ `/web` وRPC القياسيين **لم يُفحصا إطلاقًا** | فجوة حقيقية: هذا بالضبط ما يطلبه القسم 14 ("بما يشمل الروابط المباشرة") ولم يُغطَّ |
| A15 | Phase_1 §7 | CSRF، كوكيز آمنة، تدوير الجلسة | `auth_controller.py` (`csrf=True`) | PARTIAL | لا اختبار لتدوير الجلسة الفعلي | NOT_STARTED لتدوير الجلسة تحديدًا |
| A16 | Phase_1 §7/14 | إيقاف حساب يبطل الجلسات | `organization.py::suspend()`, `membership.py::_octa_revoke_sessions` | WRITTEN (أُصلح جولة 4: كان يشير لدالة غير موجودة) | NOT VERIFIED صراحة — `_invalidate_session` تخمين غير مؤكد على Odoo 19 حقيقي | **يجب التحقق من الاستدعاء الصحيح على تثبيت فعلي قبل اعتماد هذا البند مكتملًا** |
| A17 | Phase_1 §7 | حسابات خدمة POS مستقلة عن جلسات الموظفين | `branch.py::OctaHubConnection.api_key_fingerprint` | WRITTEN | NOT RUN | — |
| A18 | Phase_1 §6/14 | عزل tenant/branch عبر ORM/Controllers/RPC/export/attachments/تقارير/مهام خلفية | `record_rules.xml` (ORM فقط) | **PARTIAL — فجوة كبيرة** | ORM: NOT RUN. **RPC العام، التصدير، المرفقات (`ir.attachment`)، التقارير، المهام الخلفية: لم تُفحص ولم تُعزل بأي آلية حتى الآن** | هذا بالضبط بند إلزامي صريح في هذا التكليف (القسم 3) ولم يُغلَق — أولوية عالية للجولة القادمة |
| A19 | Phase_1 §14 | اختبار سلبي: عميل A لا يقرأ B | `tests/test_isolation.py` | WRITTEN | NOT RUN (Odoo TransactionCase) | — |
| A20 | Phase_1 §14 | مدير فرع لا يرى فرعًا آخر | `tests/test_isolation.py`, `record_rules.xml` | WRITTEN | NOT RUN | — |
| A21 | Blueprint شاشة 21/30 | لوحة أوكتاتيك لإدارة العملاء والفريق والصلاحيات | — | **NOT_STARTED** | — | الصور الحقيقية (شاشة 21، 30) تُظهر واجهة أوسع بكثير مما افتُرض: جدول عملاء كامل بحالات (نشط/قيد المراجعة/معلق)، صلاحيات لكل عضو بفروع محددة — لم يُبنَ أي جزء من هذا |
| A22 | Phase_1 §6 | سجل تدقيق (audit log) | `models/audit_log.py` (`redact()`) | WRITTEN | NOT RUN | لا ربط فعلي بعد لتسجيل كل فعل حساس تلقائيًا (لا `@api.constrains`/hook عام يكتب لهذا الجدول) |
| A23 | Blueprint شاشة 22 (جديد هذه الجولة) | جلسات نشطة قابلة للإنهاء الفردي من واجهة المستخدم | — | **NOT_STARTED** | — | الصورة تُظهر واجهة صريحة لهذا (متصفح/هاتف/جهاز مع زر "إنهاء الجلسة") — لم يُبنَ حتى نموذج بيانات لها |

## البوابة B — النقل وموثوقية الطلبات

| المعرّف | المرجع والقسم | المتطلب | مكان تنفيذه | حالة التنفيذ | دليل الاختبار | النقص والإجراء |
|---|---|---|---|---|---|---|
| B1 | Phase_1 §9 | عقد API موثّق (OpenAPI) | `docs/api/openapi.yaml` | PARTIAL | YAML صالح (تحقق فعلي) | يغطي `/orders` فقط؛ الصورة (شاشة 27) تُظهر عقدًا أوسع: `/branches`, `/orders/{id}/status`, `/catalogs`, `/availability`, حدود معدل (`60 طلب/دقيقة`) — غير موثّق |
| B2 | Phase_1 §5/14 | محاكي تطبيق + POS تجريبي مستقلان عن Odoo | `connector_demo/simulator.py`, `tools/mock_pos/` | **DONE** | **PASS 6/6** (`test_mock_pos.py`, HTTP حقيقي) | — |
| B3 | Phase_1 §9 | حفظ ذري + قيد DB يحسم التكرار المتزامن | `models/order.py::_sql_constraints` | WRITTEN | القيد نفسه NOT RUN (لا PostgreSQL)؛ المنطق المكافئ **PASS 7/7** في `lib/idempotency.py` | يحتاج اختبار تزامن فعلي ضد PostgreSQL حقيقي (INSERT متزامن يصطدم بالقيد) — النواة المعزولة تحاكي هذا فقط |
| B4 | Phase_1 §14 (إلزامي) | 20 حدث متزامن → طلب منطقي واحد | `lib/idempotency.py` | **DONE** | **PASS** (`test_20_concurrent_identical_events...`, 20 thread حقيقي) | — |
| B5 | Phase_1 §14 | نفس المفتاح محتوى مختلف = تعارض موثّق | `lib/idempotency.py` | **DONE** | **PASS** | — |
| B6 | Phase_1 §9/14 | POS يسجل ثم يقطع الرد → استعلام يؤكد | `tools/mock_pos/` | **DONE** | **PASS** عبر HTTP حقيقي (قطع اتصال TCP فعلي) | — |
| B7 | Phase_1 §9 | بلا idempotency → unknown لا فشل | `lib/idempotency.py::mark_unknown` | **DONE** | **PASS** | — |
| B8 | Phase_1 §9/14 | إلغاء قبل الإنشاء يُسجَّل لا يُهمَل | `lib/idempotency.py::apply_cancel` | **DONE** | **PASS** | — |
| B9 | Phase_1 §9/14 | تحديث قديم بعد حالة نهائية لا يرجع لتحضير | `lib/state_machine.py` + `order.py::write()` (جولة 4) | **DONE** | **PASS 11/11** (`test_state_machine.py`) للنواة؛ الحاجز الفعلي في `order.py` NOT RUN | — |
| B10 | Phase_1 §14 | 429/توكن منتهٍ تُنتج حالات حقيقية | `connector_demo/simulator.py` | **DONE** | **PASS** (`test_end_to_end_vertical_slice.py`) | — |
| B11 | Phase_1 §11/14 | توقف عامل ثم استئناف بلا تكرار أثر | — | **NOT_STARTED** | — | يحتاج queue حقيقي (`queue_job` أو بديل) بعد التحقق من توفره على Odoo 19 — مستحيل بدون تثبيت فعلي |
| B12 | Phase_1 §11 | حصص وأولوية لكل عميل/قناة | — | **NOT_STARTED** | — | يعتمد على B11 |
| B13 | Phase_1 §11 | لا تحميل كامل الطلبات بالمتصفح، لا N+1، فهارس | `order_list.js` (searchRead بحقول محددة) | PARTIAL | لا قياس أداء فعلي (لا متصفح/DB) | فهارس DB (`index=True`) موجودة في الحقول الأساسية لكن غير مُختبرة تحت حِمل |
| B14 | Phase_1 §8/14 | منع SSRF | `lib/ssrf_guard.py`, `branch.py::_check_callback_url_is_safe` | **DONE** (بعد إصلاح ثغرة ذاتية حرجة — انظر audit-findings.md#F-SEC-05) | **PASS 12/12** + تحقق شبكي حقيقي (pypi.org/localtest.me) | مطبَّق على حقل `callback_url` فقط؛ لا حقل صورة/رابط آخر بعد |
| B15 | Phase_1 §3 (هذا التكليف، جديد) | توقيع Webhook والتحقق منه، منع إعادة التشغيل (replay) وفق قدرات كل موصل | — | **NOT_STARTED** | — | **فجوة لم تُذكر بوضوح كافٍ في الجولات السابقة**: لا HMAC/توقيع أي نوع يُتحقق منه على أي webhook وارد حاليًا. `api_controller.py` يتحقق من مفتاح API فقط، لا توقيع الجسم نفسه. أولوية عالية |
| B16 | Phase_1 §3 (جديد) | فصل استلام الحدث عن حفظه عن تسليمه تقنيًا عن قبوله تجاريًا (4 مراحل مميّزة) | `models/order.py` (3 محاور حالة) + `order.event` (`event_type`) | PARTIAL | — | المحاور الثلاثة موجودة (transport/commercial/payment) لكن "الاستلام" و"الحفظ" ليسا محورين منفصلين صراحة — `event_type` يسجلهما كأحداث فقط، لا كحالة مستقلة قابلة للاستعلام السريع |
| B17 | Phase_1 §3 (جديد) | لا وصف "نجاح نهائي" لإرسال الطلب قبل دليل مناسب | `order_detail.js` (رسائل `TRANSPORT_MESSAGES`) | WRITTEN | لا اختبار آلي يفحص نصوص الواجهة | فحص يدوي فقط أن `registered_confirmed` وحدها تُعرض كنجاح؛ لا اختبار يمنع رجعيًا إضافة رسالة "نجاح" خاطئة مستقبلًا |

## البوابة C — الواجهة وتجربة الاستخدام

**تصحيح جوهري هذه الجولة:** الجدول أدناه أُعيد بناؤه بالكامل بعد مراجعة صور
التصميم الفعلية الـ11 (لم تُراجَع في أي جولة سابقة). عدد الشاشات الحقيقي في
المرجع **32 شاشة مسماة بالرقم**، لا وصفًا عامًا. القائمة الكاملة في قسم
"مصفوفة الشاشات" أسفل هذا الجدول.

| المعرّف | المرجع والقسم | المتطلب | مكان تنفيذه | حالة التنفيذ | دليل الاختبار | النقص والإجراء |
|---|---|---|---|---|---|---|
| C1 | Blueprint جدول 3 (نصي) | design tokens أولية | `design_tokens.scss` | WRITTEN، **الألوان صُحِّحت هذه الجولة** | SCSS يتجمّع فعليًا (`sass`)؛ تباين WCAG 8/8 PASS للمجموعة الفاتحة | **اكتُشف تعارض حقيقي**: الجدول النصي (جدول 3) يعطي أزرقًا #2457C5، لكن صور الشاشات الفعلية (05-08، 29-32، 01-04) تُظهر كحليًا داكنًا + teal. صُحِّح `--octa-color-primary` إلى `#0C7F71` (بعد ضبط ليجتاز WCAG AA: 4.90). القيم اللونية **تقديرية بصريًا وليست دقيقة بأداة انتقاء لون** — تحتاج تأكيد فريق التصميم |
| C2 | Blueprint شاشة 05 | نظرة عامة | `dashboard.js/xml` | WRITTEN (4 بطاقات) | لا screenshot Odoo حقيقي؛ معاينة HTML مستقلة عُرضت في المحادثة (مرتين، بالألوان القديمة والمصححة) | **تعارض داخل المرجع نفسه**: صورة "تصور واجهات المنصة" تُظهر 4 بطاقات (زمن نقل/تحتاج متابعة/تم التسجيل/مستلمة) — تطابق ما بنيناه معماريًا؛ لكن صورة أخرى لنفس الشاشة (05) في مجموعة صور مختلفة تُظهر 5 بطاقات بقيم مختلفة (متأخرة/قنوات غير متصلة/بانتظار تأكيد/معدل تأكيد%/إجمالي). **قرار**: أبقينا بناءنا الحالي (4 بطاقات، يطابق Phase_1.md §9 نصًا) وسجّلنا البديل هنا صراحة كتعارض غير محسوم من جهة العميل |
| C3 | Blueprint شاشة 06/07 | قائمة الطلبات وتفاصيلها | `order_list.js/xml`, `order_detail.js/xml` | WRITTEN | NOT RUN | الصورة الحقيقية تُظهر **timeline أفقي (stepper)** أعلى صفحة التفاصيل، بينما بنينا timeline **رأسي** داخل تبويب — كلا النمطين ظهر في نسخ مختلفة من نفس الشاشة بالمرجع (تعارض داخلي آخر) |
| C4 | Blueprint شاشة 08 | مركز الاستثناءات | `dashboard.xml` (جدول استثناءات مدمج فقط) | PARTIAL | — | الصورة الحقيقية تُظهر **شاشة مستقلة كاملة** بتبويبات (الكل/رمز منتهي/مكونات مفقودة/نظام غير متصل) — لم تُبنَ كشاشة منفصلة، فقط كجدول صغير داخل نظرة عامة |
| C5 | Phase_1 §13 بند 6 | صفحة تشغيل داخلية (Ops) | — | **NOT_STARTED** | — | الصور (شاشات 29-32) تكشف نطاقًا ضخمًا لم يُقدَّر سابقًا: لوحة تشغيل بمقاييس عمّال خلفيين، إدارة عملاء/موصلات كاملة، نظام حوادث بقوائم تنفيذ (runbook checklist)، سياسات احتفاظ بيانات وسجل اختبارات استعادة. **هذا وحده يعادل حجم مشروع Gate A كامل تقريبًا** |
| C6 | Blueprint شاشة 25-28 | بوابة الشريك التقني | — | **NOT_STARTED كواجهة** (العقد النصي فقط في openapi.yaml) | — | الصور تكشف تفصيلًا كبيرًا: صفحة عملاء الشريك بجدول تقدم ربط، صفحة مفاتيح API/webhook مع بيئة sandbox/production منفصلة، موثِّق API تفاعلي كامل، **وأداة اختبار سيناريوهات ذاتية-خدمة (self-service) للشريك تحديدًا** ("طلب مكرر" 409 Conflict) — مطابقة تمامًا لما اختبرناه نحن بـpytest لكن كواجهة مستخدم لم تُبنَ إطلاقًا |
| C7 | Blueprint شاشة 17-20 | تقارير، تسويات مالية، دعم، اشتراك | — | **NOT_STARTED** (التسويات المالية **مؤجلة صراحة في المرجع نفسه** — "قيد التطوير" مكتوبة داخل الصورة) | — | تأجيل التسويات المالية **مؤكَّد الآن كصحيح 100%** من الصورة الفعلية نفسها (وليس استنتاجًا)؛ باقي الثلاثة (تقارير/دعم/اشتراك) لم تُقيَّم بدقة كافية سابقًا |
| C8 | Phase_1 §13 | RTL/LTR وموبايل | `.o_octa_ltr_isolate` | PARTIAL | لا اختبار متصفح | صور 23/24 تُظهر تصميم موبايل مخصصًا كاملاً (ليس فقط تجاوبًا) بشريط تنقل سفلي من 5 أيقونات — لم يُبنَ أي جزء مخصص للموبايل، فقط CSS breakpoints عامة |
| C9 (جديد) | Blueprint (ظاهر في الصور) | استخدام شعارات/أسماء تطبيقات توصيل حقيقية (Keeta, Jahez, HungerStation, Ninja, ToYou, Marsool) | — | **قرار متعمَّد: لا تُستخدم** | — | هذه علامات تجارية مسجَّلة لأطراف ثالثة تظهر في صور *مرجع التصميم الداخلي* للعميل، لكن استخدام شعاراتها الفعلية في كود مُسلَّم يخالف قيود حقوق الملكية الفكرية. الكود الحالي يستخدم "demo"/أسماء عامة فقط — قرار مقصود، ليس نقصًا |

## البوابة D — التهيئة والكتالوج

| المعرّف | المرجع والقسم | المتطلب | مكان تنفيذه | حالة التنفيذ | دليل الاختبار | النقص والإجراء |
|---|---|---|---|---|---|---|
| D1 | Blueprint §12 | معالج تفعيل بخطوات قابلة للحفظ | `wizards/activation_wizard.py` | **يحتاج إعادة تصميم** | NOT RUN | الصورة الحقيقية (شاشة 04) تُظهر **قائمة تحقق (checklist) بست خطوات، كل خطوة لها مسؤول مُسمّى (شخص) ونسبة تقدم إجمالية**، مختلف جوهريًا عن حقل "step" الحالي (مؤشر خطوة واحدة فقط بلا مسؤولين ولا نسبة) |
| D2 | Phase_1 §12 | مفتاح upsert = external_id+نطاق+نوع | `tools/catalog_reconcile/reconcile.py` | **DONE** | **PASS 8/8** | — |
| D3 | Phase_1 §12/14 | لا تضاعف عند إعادة الاستيراد | نفسه | **DONE** | **PASS** | — |
| D4 | Phase_1 §12/14 | لا دمج بالاسم | نفسه | **DONE** | **PASS** | — |
| D5 | Phase_1 §12 | دفعة جزئية لا تحذف الغائب | نفسه | **DONE** | **PASS** | — |
| D6 | Phase_1 §12 | تعارض SKU مكرر بمعرف مختلف | نفسه | **DONE** | **PASS** | — |
| D7 | Blueprint §12 | نسخة أقدم لا تكتب فوق أحدث | نفسه | **DONE** | **PASS** | — |
| D8 | Phase_1 §12 (مثال حرفي) | أسعار فرع/قناة مستقلة | نفسه | **DONE** | **PASS** | صورة شاشة 11 (مصفوفة الأسعار) تؤكد هذا بالضبط بواجهة عمود-لكل-قناة — لم تُبنَ الواجهة، فقط المنطق الخلفي |
| D9 | Phase_1 §12 | ربط reconcile.py داخل Odoo (staging→معاينة→اعتماد) | `activation_wizard.py::action_run_catalog_import_preview` | PARTIAL | NOT RUN | لا يزال يستخدم store مؤقت بالذاكرة، غير موصول بجدول Odoo دائم |
| D10 | Blueprint §12 | نشر منفصل عن الحفظ بـdiff وتأكيد | — | **NOT_STARTED** | — | صورة شاشة 13 (معاينة النشر) تُظهر تفصيلًا دقيقًا: ملخص (عدد منتج/قنوات مستهدفة/فروع محدَّثة)، مقارنة سعر جديد/حالي لكل منتج بصورة مصغّرة، تحذير "مطلوب إجراء" لمنتجات ناقصة قبل الاعتماد — لا شيء من هذا مبني |
| D11 | Phase_1 §12 | إضافات (modifiers) بحد أدنى/أقصى/إلزامية | — | **NOT_STARTED** | — | صورة شاشة 10 تُظهر واجهة دقيقة لهذا بالضبط (مجموعة "اختيار الصوص"، مطلوب، حد أدنى 1 حد أقصى 2) — النموذج الخلفي غير مبني حتى الآن (لا `octa.hub.modifier.group` كموديل) |
| D12 | Phase_1 §4 | معالجة فروع (branch matching) | — | **NOT_STARTED** | — | — |
| D13 (جديد) | Blueprint شاشة 12 | مطابقة أصناف صريحة (ExternalMapping) كشاشة قائمة بذاتها | — | **NOT_STARTED** | — | مذكور في جولة 3 كمنطق فقط؛ الصورة تُظهر شاشة UI كاملة بفلتر "مطابق/غير مطابق" وتحذير عدد الأصناف غير المطابقة |
| D14 (جديد) | Blueprint شاشة 14 | سجل نتائج نشر لكل وجهة (نجح/فشل جزئي/قيد المعالجة) | — | **NOT_STARTED** | — | يطابق "نتيجة كل وجهة عند النجاح الجزئي" المطلوب صراحة في هذا التكليف (القسم 3) — فجوة مؤكدة الآن بصورة داعمة |

---

## مصفوفة الشاشات الكاملة (32 شاشة) — بعد مراجعة الصور الفعلية

| # | الاسم من المرجع | الحالة | ملاحظة |
|---|---|---|---|
| 01 | تسجيل الدخول | NOT_STARTED كواجهة (controller خلفي فقط) | ثيم داكن+teal، خريطة SA توضيحية |
| 02 | التحقق واستعادة الحساب (OTP) | NOT_STARTED | 6 خانات رمز، عداد تنازلي |
| 03 | إنشاء النشاط | NOT_STARTED | نموذج نشاط+مسؤول |
| 04 | مساعد التفعيل | **يحتاج إعادة تصميم (D1)** | checklist 6 خطوات + مسؤول لكل خطوة |
| 05 | نظرة عامة | WRITTEN (تعارض نسختين — C2) | — |
| 06 | قائمة الطلبات | WRITTEN | — |
| 07 | تفاصيل الطلب | WRITTEN (نمط timeline مختلف — C3) | — |
| 08 | مركز الاستثناءات | PARTIAL (C4) | — |
| 09 | قائمة الأصناف | NOT_STARTED كواجهة | منطق خلفي فقط (reconcile.py) |
| 10 | تفاصيل الصنف والإضافات | NOT_STARTED (D11) | — |
| 11 | مصفوفة الأسعار | NOT_STARTED كواجهة (D8 منطق فقط) | — |
| 12 | التوفر ومطابقة الأصناف | NOT_STARTED (D13) | — |
| 13 | معاينة النشر | NOT_STARTED (D10) | — |
| 14 | نتائج النشر والإصدارات | NOT_STARTED (D14) | — |
| 15 | الفروع وساعات العمل | NOT_STARTED | — |
| 16 | التطبيقات وتفاصيل الربط | NOT_STARTED كواجهة (منطق connector_demo فقط) | — |
| 17 | تقارير الخدمة | NOT_STARTED | — |
| 18 | التسويات المالية | **DEFERRED — مؤكَّد من المرجع نفسه** ("قيد التطوير") | لا حاجة لبناء أي شيء الآن |
| 19 | الدعم وتفاصيل التذكرة | NOT_STARTED | نظام تذاكر كامل |
| 20 | الاشتراك والفواتير | DEFERRED (يعتمد على نموذج تجاري لم يُحسم) | — |
| 21 | الفريق والصلاحيات | NOT_STARTED كواجهة (النموذج الخلفي `membership.py` جاهز) | — |
| 22 | الأمان والإشعارات | NOT_STARTED (A23: جلسات نشطة) | — |
| 23 | نظرة عامة على الموبايل | NOT_STARTED | تصميم مخصص، ليس تجاوبًا فقط |
| 24 | حالات الواجهة على الموبايل | NOT_STARTED | يؤكد أنماط empty/stale المبنية مفاهيميًا في dashboard.js |
| 25 | عملاء الشريك | NOT_STARTED | — |
| 26 | مفاتيح الربط والويب هوك | NOT_STARTED كواجهة (الحقل `callback_url` خلفي فقط) | بيئة sandbox/production منفصلة — غير موجودة بعد حتى كمفهوم |
| 27 | توثيق الربط | PARTIAL (openapi.yaml نص فقط، لا موثِّق تفاعلي) | — |
| 28 | الاختبار وسجل التسليم | NOT_STARTED كواجهة (السيناريوهات نفسها مُختبرة بـpytest فعليًا) | فرصة قوية: منطق B4/B5 المُختبر جاهز للعرض كأداة ذاتية-خدمة |
| 29 | لوحة التشغيل الداخلية | NOT_STARTED | نطاق ضخم (عمّال خلفيون، حوادث جماعية) |
| 30 | إدارة العملاء والموصلات (أوكتاتيك) | NOT_STARTED (A21) | — |
| 31 | الحوادث والتدقيق | NOT_STARTED | نظام incident كامل بقائمة تنفيذ |
| 32 | الاستهلاك والسياسات التشغيلية | NOT_STARTED | سياسات احتفاظ + سجل اختبار استعادة نسخ احتياطية |

**خلاصة رقمية صادقة:** من 32 شاشة، **4 مبنية جزئيًا** (05-08) بتعارضات موثّقة
مع بعض نسخ المرجع، **28 غير مبنية إطلاقًا** كواجهة (بعضها له منطق خلفي جاهز
ومُختبر). هذا رقم مختلف تمامًا عن أي انطباع أعطته الجولات 1-5 لأنها لم تكن
ترى الشاشات الفعلية أصلًا.

---

## المؤجَّل صراحة بمرجع من المستند نفسه (وليس تقديرًا)

| البند | الدليل |
|---|---|
| التسويات المالية | **صورة شاشة 18 نفسها** تحمل نص "قيد التطوير... ستكون متاحة قريبًا" — تأكيد مباشر من المصدر البصري، لا استنتاج |
| الاشتراك/الفوترة التجارية | Blueprint §15 + شاشة 20 تفترض نموذج "باقة تجريبية" غير محسوم |
| موصل حقيقي معتمد | Phase_1 §16/17 صراحة |
| توقيع Webhook لعقد حقيقي | يعتمد على وجود عقد حقيقي (غير متاح) |

## فجوات حقيقية غير محلولة (ليست تأجيلًا، بل نقص فعلي)

انظر `docs/audit-findings.md` للقائمة الكاملة مصنّفة بالشدة (Critical/High/
Medium/Low) بدل تكرارها هنا.

---

# بوابة E — توسعة OC05 (بعد مراجعة FeedUs، v1.2/v0.5، 12 سبتمبر 2026)

**تصنيف كل بند إلى حالته الحقيقية قبل أي تعديل، كما يشترط الملف الجديد
حرفيًا** ("على كلود تصنيف كل بند إلى موجود ومختبر، موجود وغير مختبر، جزئي،
أو غير منفذ قبل تغييره"). الفحص أُجري بالبحث الفعلي في الكود (`grep`) قبل
كتابة أي سطر جديد — النتائج موثّقة أدناه.

**تنبيه مصداقية:** الأعمدة "التنفيذ" و"حالة الاختبار" منفصلتان دائمًا كما
يشترط الملف — وجود كود لا يعني نجاح اختبار مستقل عليه.

| OC05 | الأولوية | التصنيف قبل التعديل | التنفيذ (بعد هذه الجولة) | حالة الاختبار | الدليل | سبب النقص |
|---|---|---|---|---|---|---|
| 01 تثبيت النطاق والهوية | P0 | موجود جزئيًا (Gate A سابقًا) | `models/organization.py`, `models/invitation.py`, ACL `perm_create=0` | NOT RUN (Odoo) | — | يحتاج Odoo فعلي لتأكيد رفض الطلب المباشر |
| 02 نطاق العمل الثابت (tenant/brand/branch) | P0 | **brand_id غير منفذ إطلاقًا** | **جديد هذه الجولة**: `models/organization.py::OctaHubBrand`, `models/branch.py::brand_id` + قيد تطابق tenant | NOT RUN | py_compile نظيف، ACL متسق (تحقق تلقائي) | لا اختبار Odoo فعلي؛ لا واجهة رأس صفحة تعرض السياق بعد |
| 03 مركز تفعيل التاجر | P0 | موجود جزئيًا وناقص + **bug حرج مكتشف**: التقدّم كان مخزَّنًا على TransientModel (يُحذف تلقائيًا) | **أُعيد التصميم فعليًا هذه الجولة**: `models/activation.py` (نموذجان دائمان جديدان: `octa.hub.activation.checklist` و`.step`) + إصلاح `wizards/activation_wizard.py` ليستخدمهما | `lib/activation_checklist.py` **PASS 7/7** (منطق معزول)؛ النماذج الفعلية NOT RUN | `test_activation_checklist.py` | الواجهة (شاشة 04) لا تزال NOT_STARTED؛ الربط الفعلي بـOdoo حقيقي NOT VERIFIED |
| 04 دليل الموصلات ومصفوفة القدرات | P0 | موجود شكليًا (قاموس ثابت بلا إنفاذ فعلي) | **جديد**: `lib/connector_capabilities.py` (ConnectorDefinition/MerchantConnection/BranchChannelBinding منفصلة، إنفاذ حقيقي) | **PASS 6/6** | `test_connector_capabilities.py` | الربط بـsimulator.py القديم وبموديل Odoo فعلي NOT_STARTED |
| 05 توسيع المطابقة | P0 | موجود جزئيًا (CatalogKey في reconcile.py محصور بالكتالوج) | **جديد**: `lib/external_mapping.py` (MappingKey يعمم لـ8 أنواع كيانات) | **PASS 6/6** | `test_external_mapping.py` | لا موديل Odoo بعد؛ لا ربط بـPOS حقيقي |
| 06 جودة استيراد الكتالوج | P0 | **موجود ومختبر بالفعل** (Gate D سابقًا) | `tools/catalog_reconcile/reconcile.py` — لم يتغيّر | **PASS 10/10** (كان 8) | `test_reconcile.py` | ImportBatch ككائن صريح بـsource_version/snapshot_mode لم يُضَف بعد كحقل منفصل |
| 07 استكمال تفاصيل الصنف | P1 | غير منفذ | لم يُبنَ هذه الجولة | NOT RUN | — | فجوة حقيقية — لا حقول تغذية/حساسيات/حد أقصى وصف لكل قناة |
| 08 مصفوفة أسعار الفروع والتطبيقات | P0 | غير منفذ (كان منطق أسعار مبسّط فقط في reconcile.py دون أولوية حل صريحة) | **جديد**: `lib/price_resolution.py` (أولوية فرع+قناة > قناة > فرع > أساس، Decimal، لا تحويل عملة صامت) | **PASS 9/9** بما فيها **مثال القبول الحرفي بالضبط** (كبدة 10/12/14/15/9) | `test_price_resolution.py` | لا موديل Odoo `PriceOverride` بعد؛ لا واجهة |
| 09 الخصومات المجدولة | P1 | غير منفذ | لم يُبنَ هذه الجولة | NOT RUN | — | فجوة حقيقية |
| 10 قواعد الإضافات | P0 | غير منفذ | **جديد**: `lib/modifier_rules.py` (min/max/mandatory/allow_repeat، فصل عدد الخيارات عن الكمية) | **PASS 9/9** بما فيها أمثلة القبول الحرفية (حجم واحد إجباري، تكرار صوص) | `test_modifier_rules.py` | لا موديل Odoo `octa.hub.modifier.group` بعد |
| 11 التوفر المطلوب والمؤكد | P0 | غير منفذ (لم يكن desired/confirmed منفصلين) | **جديد**: `lib/availability_state.py` | **PASS 10/10** بما فيها partial وstale وحماية من تحديث قديم | `test_availability_state.py` | لا موديل Odoo بعد؛ لا ربط webhook حقيقي |
| 12 تشغيل الفرع والقبول الآلي | P1 | غير منفذ | لم يُبنَ هذه الجولة | NOT RUN | — | فجوة حقيقية |
| 13 النشر والمقارنة وسجل النسخ | P1 | غير منفذ (كان D10 مسجّلاً NOT_STARTED سابقًا) | لم يُبنَ هذه الجولة | NOT RUN | — | فجوة حقيقية، أولوية تالية |
| 14 رحلة الطلب وإثبات التسجيل | P0 | موجود جزئيًا (3 محاور حالة + order.event عام، بلا فصل اتجاه أمامي/عكسي صريح) | **جديد**: `lib/order_trace.py` (ForwardEvent/ReverseEvent منفصلان، إثبات أن HTTP 200 لا يساوي pos_registered) | **PASS 6/6** | `test_order_trace.py` | لا ربط بـ`octa.hub.order.event` الفعلي بعد — الحقول الصريحة لكل مرحلة لم تُضَف كأعمدة Odoo |
| 15 إعادة المعالجة الفردية والجماعية | P0 | غير منفذ | **جديد**: `lib/reprocessing.py` (معاينة أهلية، attempt_id منفصل، rate limiter لكل تاجر/قناة، **يعيد استخدام idempotency.py الحقيقي** لا منطقًا مكررًا) | **PASS 7/7** بما فيها اختبار تزامن حقيقي (10 threads) | `test_reprocessing.py` | لا موديل Odoo ولا واجهة أزرار فردي/جماعي بعد |
| 16 الطلبات المجدولة | P1 | غير منفذ | لم يُبنَ هذه الجولة | NOT RUN | — | فجوة حقيقية |
| 17 مؤشرات الموثوقية والتوقيت | P0 | موجود جزئيًا وضعيف (p95 كان `None` ثابتًا بلا حساب) | **جديد**: `lib/reliability_metrics.py` + **وُصل فعليًا** بـ`order.py::get_dashboard_metrics` (لم يعد عنصرًا نائبًا) | **PASS 7/7** بـfixtures بتوقيتات معروفة (كما يشترط القبول حرفيًا) | `test_reliability_metrics.py` | الربط بحقول Odoo فعلية (توقيت كل مرحلة) لا يزال تقريبيًا (يستخدم write_date/create_date كبديل مؤقت، NOT VERIFIED دقته) |
| 18 مركز الاستثناءات والتنبيه | P1 | موجود جزئيًا (جدول صغير داخل dashboard.xml فقط) | لم يتغيّر هذه الجولة | NOT RUN | — | فجوة كبيرة معروفة من قبل (C4/C5) |
| 19 التقارير التجارية | P1/P2 | غير منفذ (P1)، خارج النطاق (P2) | لم يُبنَ هذه الجولة | NOT RUN | — | فجوة حقيقية للـP1؛ P2 مؤجل بحق |
| 20 مواصفات الشاشات المحدثة | P1 | موجود جزئيًا (3 شاشات فقط من 32) | لم تُبنَ شاشات جديدة هذه الجولة (تركيز الجولة على منطق الأعمال المُختبَر) | NOT RUN | — | لا screenshot حقيقي على الإطلاق — لا متصفح Odoo |
| 21 ترحيل الكود والبيانات | P0 | **لم يُوثَّق كملف مستقل من قبل** | **جديد**: `docs/migrations.md` (انظر أدناه) | N/A — لا قاعدة بيانات فعلية للترحيل عليها | — | يحتاج Odoo+PostgreSQL حقيقيين لتُختبر عليهما migration فعلية |
| 22 الأداء والتسليم بالأدلة | P0 | موجود جزئيًا (اختبارات إجهاد pure-Python فقط، لا بيئة/موارد معلنة رسميًا) | بيئة موثّقة في `docs/test-results.md` | جزئي — إجهاد pure-Python **PASS** (200 thread، 20 HTTP حقيقي)؛ **لا اختبار حمل حقيقي ضد Odoo/PostgreSQL** | `docs/test-results.md` | يحتاج Odoo حقيقي لقياس p95 فعلي لحفظ/إقرار طلب Odoo — النواة المعزولة لا تقيس هذا |

## ملخص رقمي صادق لبوابة E

- **10 من 22 بندًا حصلت على تنفيذ ومنطق مُختبَر فعليًا** عبر جولتي العمل (04، 05، 06، 08، 10، 11، 17، ثم 03، 14، 15 في جولة المتابعة).
- **إجمالي اختبارات Gate E الجديدة: 67** (9+9+10+6+6+7 من الجولة الأولى + 6 رحلة الطلب + 7 إعادة معالجة + 7 مركز تفعيل من جولة المتابعة).
- **إجمالي الاختبارات الحقيقية في المشروع كله الآن: 131** (كان 57 قبل Gate E)، عبر `python3 tools/run_all_tests.py`. راجع `docs/audit-findings.md` قسم "مراجعة عاشرة" لـ3 تعارضات عقود حقيقية بين الموديولات اكتُشفت وأُصلحت (قدرات الموصل، أحداث الطلب، عرض مؤشرات التوقيت)، و4 اختبارات تقريب عملة جديدة (KWD/JPY كحالات حقيقية مختلفة عن الافتراض الشائع بخانتين).
- **12 من 22 بندًا لا تزال NOT_STARTED كليًا أو جزئيًا كبيرًا** (07، 09، 12، 13، 16، 18، 19، 20 الشاشات، إلخ) — مذكورة صراحة، لا إسقاط صامت لأي منها.
- **bug حرج اكتُشف وأُصلح أثناء تنفيذ OC05-03**: معالج التفعيل القديم (Gate D) كان يخزّن كل تقدّم التفعيل على `TransientModel` — وهذه تُحذف تلقائيًا في Odoo (`ir.autovacuum`)، يخالف مباشرة "الحفظ والاستكمال آمنان". أُصلح بنموذجين دائمين جديدين. انظر `docs/audit-findings.md`.
- **لا شيء من هذا مربوط بموديل Odoo فعلي مُشغَّل** — كل الجديد pure-Python مُختبَر بمعزل عمدًا، بنفس نمط idempotency.py/reconcile.py الناجح سابقًا. الربط بـOdoo نفسه يبقى NOT_STARTED حتى تتوفر بيئة Odoo حقيقية.

---

## تحديث المراجعة الحادية عشرة — مراجعة قبول نهائية (فحص ملف-بملف فعلي)

فُكَّ آخر ZIP مُسلَّم فعليًا (بصمة SHA-256:
`abf1c05e26a2dd5aa7e68a8992587cfbd1f050e06918790f1ac5921765bbda8c`) في
مسار معزول، وقورن ببصمة (`diff -rq` صفر اختلاف عن مجلد العمل)، وأُعيدت
اختباراته من المسار المعزول نفسه (131/131 PASS). راجع
`docs/file-review-register.md` (ملف جديد، مطلوب صراحة) للتفاصيل الكاملة
لكل ملف. 5 مشاكل حقيقية جديدة اكتُشفت وأُصلحت (تفاصيل في
`docs/audit-findings.md` قسم "مراجعة حادية عشرة"):
- `except` عام في `api_controller.py` يخفي أخطاءً حقيقية (F-ERR-01).
- `enforce_capability` غير موصول بنقطة الاستقبال الفعلية (F-WIRE-01).
- 5 حالات `ValueError` بدل `ValidationError` (F-EXC-01).
- حقل `superseded_by_id` معرَّف ولا يُكتب (F-FIELD-01).
- لا مسار لاستعادة كلمة المرور رغم دعم الموديل الكامل (F-ROUTE-01).

**لا تغيّر عددي في الاختبارات (131 ثابتة)** — كل الإصلاحات هذه الجولة في
كود Odoo NOT RUN، تحقّقها عبر py_compile/XML صالح + إعادة تشغيل كل
الاختبارات القائمة (لا كسر شيء)، لا اختبارات pytest جديدة مباشرة لها.

---

## تحديث المراجعة الثانية عشرة — استجابة لمراجعة مستقلة خارجية حقيقية

لأول مرة، تم تنزيل مصدر Odoo 19 الحقيقي فعليًا والتحقق المباشر من كل
ملاحظة تقنية بدل الاعتماد على معرفة نظرية. **12 من 16 ملاحظة (R01-R16)
مؤكَّدة بدليل مصدري وأُصلحت** — أخطرها R03: `_sql_constraints` غير مدعومة
فعليًا في Odoo 19 (تحذير + تجاهل صامت للقيد)، ما كان يعني أن **القيد
الأهم في كامل تصميم منع تكرار الطلبات** لم يكن ليُطبَّق أبدًا على أي
تثبيت حقيقي. التفاصيل الكاملة والدليل المصدري المباشر لكل بند في
`docs/audit-findings.md` قسم "مراجعة ثانية عشرة".

**تغييرات جوهرية بهذه الجولة:**
- `models.Constraint` تحل محل `_sql_constraints` في 6 ملفات (8 قيود).
- `fields._tz_get` → استيراد صحيح من `odoo.addons.base.models.res_partner`.
- `res.groups.category_id` → `res.groups.privilege_id` + نموذج `res.groups.privilege` جديد.
- 7 قواعد عزل جديدة (`connection`, `brand`, `membership`, `activation.*`, `order.event`, `delivery.attempt`).
- `api_controller.py`: إعادة كتابة كاملة بـ`type="http"` بدل `type="json"` المهجورة، مقارنة محتوى حقيقية، `cr.savepoint()`.
- `invitation.py::preview_only`: إصلاح bug `is None` مقابل `False` (يفشل دائمًا كان).
- `invite_accept`: يفعّل حسابًا حقيقيًا الآن بدل نجاح واجهة وهمي.
- `tests/__init__.py`: استُعيد (كان قد أُزيل جولة 7) لتوافق اكتشاف اختبارات Odoo، بعد تأكيد عدم تعارضه مع عزل `run_all_tests.py`.
- `octa_hub_ui`: `ir.actions.client` + `menuitem` حقيقيان جديدان يربطان الشاشات الثلاث بتنقّل Odoo فعلي (كانت غير موصولة إطلاقًا).

**لم يُحسم:** R05 (أمان مفتاح API)، R12 (capability enforcement بتعريف مؤقت)، R13 (لا queue/worker حقيقي) — موثَّقة صراحة، لم يتسع الوقت لحلها هذه الجولة.

**إجمالي الاختبارات: 135 (كان 131)**، عبر `python3 tools/run_all_tests.py`.

---

## تحديث المراجعة الثانية عشرة — مراجعة مستقلة خارجية حقيقية + تحقق من مصدر Odoo 19

استُلم تقرير مراجعة مستقل خارجي (`Octa_Connect_Independent_Review_AR.md`)
يحمل بصمة نسخة محددة (SHA-256 موثَّقة في التقرير نفسه) و16 ملاحظة (R01-R16)
مبنية جزئيًا على **تنزيل فعلي لمصدر Odoo 19 الحقيقي** من جهة المراجع
المستقل. بدل قبول أو رفض الملاحظات بلا دليل، **استُنسِخ نفس مصدر Odoo 19**
هنا فعليًا (`git clone --depth 1 --branch 19.0`) وتحقَّقت كل ملاحظة حرجة
مباشرة من الكود الحقيقي قبل أي إصلاح.

**النتيجة: 12 من 16 ملاحظة صحيحة 100% بدليل مصدري مباشر، وأُصلحت جميعًا:**
R01 (`fields._tz_get` غير موجودة)، R02 (`res.groups.category_id` استُبدل
بـ`privilege_id`)، R03 (**الأخطر**: `_sql_constraints` مُتجاهَلة صامتًا
بالكامل في Odoo 19 — كل قيد تفرّد في المشروع، بما فيها منع تكرار الطلب،
لم يكن سيُنشأ فعليًا)، R04 (7 موديلات بلا عزل رغم ACL)، R06 (تفعيل حساب
وهمي)، R07 (**`rec.consumed_at is None` يفشل دائمًا** — مُتحقَّق حرفيًا من
`Field.convert_to_record`)، R08 (رمز استعادة كلمة مرور يُهمَل)، R09
(`type="json"` لا يضبط حالة HTTP فعليًا)، R10 (لا مقارنة محتوى، لا
savepoint)، R11 (`str(items)` ليست JSON)، R14 (خلط نوافذ زمنية)، R16
(اكتشاف اختبارات Odoo كان سيفشل تمامًا).

**3 ملاحظات سدَّت جزئيًا فقط، لم تُحل جذريًا (موثَّقة صراحة NOT VERIFIED):**
R05 (مقارنة مفتاح API)، R12 (تعريف موصل مؤقت)، R15 (UI موصولة الآن لكن
الاختبار الفعلي على متصفح حقيقي لا يزال NOT RUN).

**1 ملاحظة غير مُعالَجة إطلاقًا:** R13 (لا queue/worker حقيقي).

**إجمالي الاختبارات الآن: 135 (كان 131)، عبر `python3 tools/run_all_tests.py`.**
راجع `docs/audit-findings.md` قسم "مراجعة ثانية عشرة" للتفاصيل الكاملة لكل
بند مع الاستشهاد الدقيق من مصدر Odoo 19.

**تنويه صادق (كما ينص التكليف حرفيًا):** نجاح `py_compile`/XML/JS syntax
**لا يثبت تشغيل Odoo فعليًا** — هذه الإصلاحات مبنية على قراءة مصدر Odoo 19
الحقيقي (أقوى دليل متاح بدون Odoo مُثبَّت فعليًا)، وليست نتيجة تشغيل
اختبار قبول حقيقي على تثبيت حي. البيئة لا تزال بلا PostgreSQL/root (نفس
القيد الذي واجهه المراجع المستقل نفسه).

---

## تحديث المراجعة الثالثة عشرة — رد فعلي على مراجعة V2 مستقلة

استُلمت مراجعة V2 (`Octa_Connect_Review_V2_AR.md`) قدّمت أدلة تنفيذية حقيقية
لا نصية فقط: تشغيل runner فعلي (135 PASS من نسختها بعد إعادة تثبيت pytest)،
واستخراج AST فعلي لدالة `receive_order_event` وتشغيلها بمحاكيات ORM/request
لإثبات **مسار نجاح كاذب فعلي** (200 + order_id=None عند IntegrityError بلا
سجل مطابق). كل ملاحظة حرجة تحقَّقت بدليل مباشر (مصدر Odoo 19 الحقيقي أو
تجربة فعلية) قبل أي إصلاح — راجع `docs/audit-findings.md#مراجعة-ثالثة-عشرة`
للتفاصيل الكاملة.

**أُصلح جذريًا (بدليل)**: نجاح الاستقبال الوهمي (تضييق لـUniqueViolation +
تحقق اسم القيد + رفض 500 بدل 200 مزيَّف عند غياب سجل مطابق)؛ اختبارات Odoo
(`groups_id`→`group_ids`)؛ عزل تفويض الشريك (expires_at/branch_ids عبر حقل
مخزَّن، لأن ir.rule.domain_force لا يملك datetime في سياقه — مُتحقَّق من
المصدر)؛ تصحيح خطأ توثيقي حرج عن دلالات OR/AND بين قواعد Odoo (مُتحقَّق من
`ir_rule.py::_compute_domain` الحقيقي)؛ تقييد purpose في استهلاك الدعوة +
قفل ذري حقيقي (`lock_for_update()`، مُتحقَّق من المصدر)؛ إنشاء `mail.mail`
حقيقي بدل تسجيل log فقط؛ دالة تجزئة مفتاح API فعلية (لم تكن موجودة إطلاقًا)؛
إصلاح عدم تطابق شكل بيانات Timeline (كانت ستعرض عناصر فارغة تمامًا).

**لم يُعالَج**: R12 (تعريف قدرة مؤقت)، R13 (لا queue/worker حقيقي — أكبر
فجوة بنيوية متبقية)، عمق إصلاح R14 (p95 لا يزال تقريبًا خامًا)، اتحاد أدوار
التاجر عبر OR (خطر حقيقي موثَّق، غير محلول).

**إجمالي الاختبارات الآن: 145 (كان 135)، عبر `python3 tools/run_all_tests.py`
(6 مجموعات الآن، لا 5).**

**تحقق هوية النسخة**: بصمة الأرشيف المختلفة بين الجولتين تحقَّقت شخصيًا
كفرق غلاف زيب فقط (صفر اختلاف محتوى فعلي، `diff -rq`). أُضيف
`docs/SOURCE_MANIFEST.sha256` (بصمة كل ملف مصدري على حدة) لحل هذا اللبس
نهائيًا لأي مراجعة قادمة.

---

## تحديث المراجعة الرابعة عشرة — إغلاق الفجوات الأربع من نهاية الجولة الثالثة عشرة

طلب صريح بمتابعة الفجوات المتبقية وإغلاقها. النتيجة:

| الفجوة | الحالة | الدليل |
|---|---|---|
| اتحاد أدوار التاجر عبر OR (owner+branch_manager) | **CLOSED** | `lib/authority_context.py` (10/10 PASS) موصول بقيد `membership.py` |
| R12 (تعريف قدرة مؤقت يفترض ORDERS مدعومة دائمًا) | **CLOSED** | نموذجا Odoo دائمان جديدان + بيانات حقيقية + `_enforce_orders_capability` تقرأ منهما |
| R13 (لا queue/worker حقيقي) | **PARTIAL** | نمط outbox كامل مُختبَر (8/8 PASS، منها تزامن 20 thread حقيقي) + نموذج دائم + cron حقيقي + الاستقبال يُدرج فعليًا الآن — لكن **الإرسال الفعلي لنظام POS من داخل العامل لا يزال NOT_STARTED** |
| R14 عمق (p95 من write_date/create_date) | **CLOSED جزئيًا** | حقلا توقيت صريحان جديدان يُسجَّلان عند لحظة الانتقال الفعلية، لا الميتاداتا العامة |

**إجمالي الاختبارات الآن: 157 (كان 145)، عبر `python3 tools/run_all_tests.py`
(6 مجموعات، لا تغيير في العدد لكن المحتوى داخل `octa_hub_api/lib` زاد
من 5 إلى 13 اختبارًا).**

**اكتشاف ذاتي أثناء إغلاق R13** (قبل أي مراجعة خارجية): المسودة الأولى
لإدراج عنصر Outbox في `api_controller.py` كانت تلتقط استثناء الإدراج
وتسجّله كتحذير فقط — أُصلحت ذاتيًا فور ملاحظة أن هذا يُبقي الطلب محفوظًا
بلا عنصر طابور للأبد بلا أي آلية اكتشاف، وهو تناقض بيانات صامت أخطر من
فشل الطلب كله بوضوح. لا try/except حول الإدراج الآن — يتبع نمط
transactional outbox الصحيح (ينجحان أو يفشلان معًا ذريًا).

**لا يزال غير مكتمل صراحة**: الإرسال الفعلي لنظام POS من داخل العامل
(R13 جزء التكامل)، تغطية كاملة لكل مراحل §10 الزمنية (R14).

---

## تحديث المراجعة الخامسة عشرة — تدقيق ذاتي على كود R12/R13 من الجولة السابقة

بدل نطاق جديد، راجعت الكود الذي كتبته بنفسي في الجولة السابقة مباشرة بعناية
أكبر، ولقيت مشكلتين حقيقيتين وأصلحتهما:

- **لا ربط تلقائي بين اتصال جديد وتعريف قدرته**: كان `connector_definition_id`
  (R12) سيبقى فارغًا لكل اتصال حقيقي جديد، فيُرفَض كل طلب له فورًا. أُصلح
  بربط تلقائي ذكي عند الإنشاء (deny-by-default يبقى فعّالًا لو لم يوجد
  تعريف مطابق).
- **استخدام `lock_for_update()` الخاطئة في سياق دفعة (R13)**: أعدت التحقق
  من مصدر Odoo 19 الحقيقي بعناية أكبر ووجدت أن هذه الدالة ترفع استثناءً
  **للدفعة كاملة** لو عنصر واحد كان مقفولًا من عامل آخر — يخالف تمامًا نمط
  claim-what's-available المطلوب لطابور. أُصلح باستخدام `try_lock_for_update()`
  الصحيحة فعليًا لهذا النمط (موثَّقة حرفيًا في المصدر لهذا الغرض بالذات).

**إجمالي الاختبارات: 157 (بلا تغيير عددي — كلا الإصلاحين بنيويان في كود
Odoo NOT RUN؛ التحقق عبر py_compile + مصدر Odoo 19 + إعادة تشغيل الاختبارات
القائمة).**

---

## تحديث جوهري — الجولة العشرون: أول تشغيل حقيقي فعلي على Odoo 19 + PostgreSQL

**تصحيح افتراض بيئي كان مُكرَّرًا عبر 19 جولة**: البيئة تعمل فعليًا
بصلاحيات `root` كاملة؛ `sudo: not found` كان يعني غياب أداة `sudo`
تحديدًا، لا غياب الصلاحيات. تثبيت PostgreSQL 16 واعتماديات Odoo 19
الحقيقية نجح بالكامل. **كل الأربع موديولات (`octa_hub_core`,
`octa_hub_api`, `octa_hub_connector_demo`, `octa_hub_ui`) ثُبِّتت بنجاح
فعليًا على Odoo 19 حقيقي، وكل الـ29 اختبار Odoo TransactionCase المكتوبة
عبر الجولات السابقة (والتي كانت مُعلَّمة NOT RUN دائمًا) شُغِّلت فعليًا:
0 failed, 0 error(s).**

هذا التشغيل الفعلي كشف **9 عيوب حقيقية** لم تكتشفها 19 جولة مراجعة سابقة
ولا مراجعتان مستقلتان خارجيتان دقيقتان جدًا — تفاصيل كاملة في
`docs/audit-findings.md#مراجعة-عشرون`، أبرزها:
- موديول كامل (`octa_hub_connector_demo`) كان بلا `__manifest__.py` منذ
  الجولة الأولى، فكان Odoo يرفضه بصمت.
- نظام الدعوات بأكمله كان معطوبًا كليًا (`create()` بديكوريتر Odoo قديم بائد).
- **الأهم أمنيًا**: لا ربط كان موجودًا بين نموذج العضوية المخصص وصلاحيات
  Odoo الفعلية (`res.groups`) — يعني منح "عضوية" لم يكن يمنح صلاحية
  حقيقية إطلاقًا طوال 19 جولة، رغم كل التوثيق الذي افترض عكس ذلك.

**تعليمات التثبيت والاختبار الحقيقية المُتحقَّقة موثَّقة بالكامل في
`docs/RUNBOOK.md`.**

هذا **لا يعني اكتمال المشروع** — عشرات البنود لا تزال NOT_STARTED كما
هو موثَّق في `docs/hungerstation-gap-review.md` وغيره، ولا تكامل POS
إنتاجي حقيقي. لكن الأساس البنيوي الآن مُثبَت أنه يعمل فعليًا، لا نظريًا.
